# Bruce — the ITCM boot copy and `thunk_EXT_FUN_0000xxxx` resolution (session 5)

Decompiled C for every function cited here is in `analysis/decomp/`. Ghidra project regenerable per `bruce-ghidra.md`. New script: `analysis/ghidra_scripts/MapItcmAddr.java`.

## Confirmed: the copy loop

`Reset_Handler` (`0x60040500`) decompiles cleanly (`analysis/decomp/Reset_Handler.c`). After a soft-reset check, it runs a sequence of standard Cortex-M/CMSIS startup copy loops before jumping to the real init code. The relevant one:

```c
puVar4 = &DAT_6004081c;
for (puVar6 = (undefined4 *)0x0; (int)puVar6 < 0xbdf8; puVar6 = puVar6 + 1) {
  *puVar6 = *puVar4;
  puVar4 = puVar4 + 1;
}
```

i.e. a word-at-a-time `memcpy(dst=0x00000000, src=0x6004081c, size=0xbdf8)`:

- **Source (flash):** `0x6004081c` – `0x6004c614` (file offset `0x81c` – `0xc614` in `bruce_pvt_a_prod_signed.bin`)
- **Destination:** `0x00000000` – `0x0000bdf8` (ITCM, per CLAUDE.md's working theory)
- **Size:** `0xbdf8` = 48,632 bytes (~47.5 KiB), copied as 12,158 words

**Mapping formula:** `flash_addr = itcm_addr + 0x6004081c`, for `itcm_addr` in `[0, 0xbdf8)`. Automated by the new script `MapItcmAddr.java` (also accepts `thunk_EXT_FUN_0000xxxx` names directly).

The constants were cross-checked against the image's literal pool right after `Reset_Handler` (file offset `0x60c`/`0x610`/`0x614`, dumped with `xxd`): word at `0x60040610` = `0x00000000` (dest), `0x6004060c` = `0x6004081c` (src), `0x60040614` = `0x0000bdf8` (size) — all three match the decompiled constants exactly. The same literal pool also confirmed the adjacent loops: a `.data` copy (flash `0x6013d5a8` → RAM `0x20002740`–`0x20003fd8`), a `.bss` zero (RAM `0x20004020`–`0x2002a824`), a stack-paint fill (`0xAABBAABB` from `0x2002a828` to `SP-0x40 = 0x2002b7e8`, SP itself = `0x2002b828`, matching the SP already noted in `bruce-ghidra.md`), and finally a call through `*0x6004062c` = `0x6004c701` → `FUN_6004c700`, the real second-stage init (already listed as a `Reset_Handler` callee). `FUN_6004c700`'s block starts at flash `0x6004c700`, right after the copied ITCM region ends (`0x6004c614`) — the linker placed the ITCM-resident blob as one contiguous chunk immediately before normal `.text`, which is exactly what you'd expect from an `.itcm`/`.ramfunc` linker-script section.

## Confirmed: how call sites reach ITCM code (the veneer table)

`thunk_EXT_FUN_0000xxxx` isn't a raw address Ghidra invented — it's the *name* Ghidra gives after auto-resolving a real two-stage call:

1. Call sites `BL` to a small **veneer** function living at a normal, reachable flash address (mostly clustered at `0x6013cee8`–`0x6013d4e0`, right near the end of the image; a handful of earlier ones sit right after `Reset_Handler`, e.g. `0x60040644`).
2. The veneer is 4–8 bytes of code that loads the absolute ITCM target and branches to it — e.g. at `0x6013cee8`: `5f f8 00 f0` = Thumb-2 `LDR.W PC,[PC,#0]`, immediately followed by the literal word `0000af91` (i.e. target `0x0000af90`, Thumb bit set). Earlier veneers use the 4-byte Thumb-1 idiom `LDR r0,[pc,#N]; BX r0` instead (e.g. `0x60040644` → literal at `0x60040748` = `0x00000e51` → target `0x00000e50`).

Ghidra's function/thunk analysis walks through both hops and reports the *ultimate* low address in the name (`thunk_EXT_FUN_0000xxxx`), and `Function.getCallingFunctions()` already resolves through the whole chain to the real caller — no manual veneer-chasing needed once you have the ITCM→flash mapping.

**`bruce_functions.csv` lists 155 such veneers**, targeting ITCM offsets from `0x348` to `0xb5ca` — every single one falls inside the `[0, 0xbdf8)` range copied by the loop above, which is strong independent confirmation this is the right copy. Ghidra's ordinary auto-analysis had already disassembled **235 functions** inside the flash-resident copy source range `0x6004081c`–`0x6004c614` (it's just contiguous code in the image); resolving a thunk is therefore just "decompile the function at the mapped flash address" — **no Ghidra overlay/new memory block was needed.**

## Resolved thunk targets (10, via `MapItcmAddr.java` + `Decompile.java`)

18 of the 235 in-blob functions already carry `__FILE__`-string attribution (`bruce_srcmap.csv`): **7 to `queue.c`, 9 to `tasks.c`, 2 to `heap_5_improved.c`** — i.e. the blob is, as suspected, FreeRTOS's hottest primitives plus a HAL layer. Resolved by decompiling the mapped flash address:

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000737c` | `0x737c` | `0x60047b98` | **`tasks__60047b98`** (src: `tasks.c`) — checks a flag, calls a queue/notify helper and two more `tasks.c` routines, and on one path writes `0x10000000` to a MMIO register then `DSB`/`ISB` — the classic Cortex-M `SCB->ICSR = PENDSVSET` pattern FreeRTOS uses to force a context switch. Matches the "ring a hardware doorbell" idiom already seen in `hid_input_target` (`bruce-io-paths.md`). |
| `thunk_EXT_FUN_0000b5ba` | `0xb5ba` | `0x6004bdd6` | **`memset`**-shaped: byte-fill loop over `[param_1, param_1+param_3)` with `param_2`. |
| `thunk_EXT_FUN_0000b572` | `0xb572` | `0x6004bd8e` | **`memcpy`**-shaped: byte-copy loop, `param_1 ← param_2`, length `param_3`. |
| `thunk_EXT_FUN_0000af88` | `0xaf88` | `0x6004b7a4` | Thin wrapper: `if (len != 0) memcpy(...)` (calls the `memcpy` above). |
| `thunk_EXT_FUN_0000af90` | `0xaf90` | `0x6004b7ac` | Sibling wrapper: `if (len != 0) memset(...)` (inlined fill loop, same shape as the standalone `memset`). |
| `thunk_EXT_FUN_0000ac52` | `0xac52` | `0x6004b46e` | **`bzero`/`OPENSSL_cleanse`**-shaped: `if (len != 0) { zero-fill loop }`. Corroborates `bruce-crypto.md`'s inference (`FUN_600ec31c`'s one-shot `SHA512()` calls this thunk to wipe its stack context). |
| `thunk_EXT_FUN_0000714c` | `0x714c` | `0x60047968` | Trivial 6-byte getter: `return *DAT_60047970;` (single global word — likely a tick-count/state accessor). |
| `thunk_EXT_FUN_00007c58` | `0x7c58` | `0x60048474` | Lock-guarded field read: `lock(); v = *(obj+0x20); unlock(); return v;` — a mutex-protected accessor. |
| `thunk_EXT_FUN_00007a2c` | `0x7a2c` | `0x60048248` | **Bus/queue transaction** (already inferred in `bruce-io-paths.md` from call-site shape `(handle, cmd, arg, out, timeout)`); decompiles to exactly that — branches on `param_2` and calls **`queue__60047290`**/**`queue__600473e4`** (both `src: queue.c`), i.e. it's a thin front-end over two FreeRTOS-queue primitives. |
| `thunk_EXT_FUN_00001ea4` | `0x1ea4` | `0x600426c0` | **I2C transfer** (already inferred in `bruce-io-paths.md`). Fully confirmed here: references string literals `"i2c.cc"`, `"Delaying I2C until startup time"`, and `"I2c operations only supported af[ter startup]"` right in the decompiled body. |

## Resolved thunk targets, continued (session 6: full remaining sweep)

All 132 remaining distinct `thunk_EXT_FUN_0000xxxx` names (every one left after session 5's initial 10) were mapped via `MapItcmAddr.java` and decompiled via `Decompile.java` in this session -- **every distinct thunk target `bruce_functions.csv` lists is now resolved (142 of 142)**. They collapse to 127 distinct functions. A handful of thunks target addresses that aren't the canonical entry point of their resolved function; **this session's write-up originally described all of these as "the function's body is split/discontiguous," but a QA pass (verifying against the raw disassembly, since two different mechanisms produce the same Ghidra-level symptom) found that's only true for one case.** See the corrected methodology note under `FUN_60042c9c` below (a genuine two-region function body) versus the note under `heap_5_improved__60048774`/`heap_5_improved__600488f4` (which turned out to be ordinary chained branch-thunks, not split bodies). Grouped here by subsystem rather than address for readability; all columns match the table above.

### FreeRTOS core (critical sections, list.c, PendSV handler, scheduler core, queue.c/event_groups.c internals, spinlock/once primitives) (40 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00000e50` | `0xe50` | `0x6004166c` | **`FUN_6004166c`** -- **FreeRTOS `xPortPendSVHandler` (Cortex-M7 context-switch trampoline).** Tests `EXC_RETURN` bit 4 to decide whether to manually stack `s16-s31` (lazy FPU stacking), pushes `r4-r11`+`lr` onto the process stack, raises `BASEPRI` to `0x50` (`configMAX_SYSCALL_INTERRUPT_PRIORITY`), calls `func_0x60047be8(0x50)` (the scheduler -- see next row), drops `BASEPRI` back to 0, and returns the new process-stack pointer for the exception return sequence. This is the actual PendSV ISR that performs the switch that `tasks__60047b98` (session 5) requests by setting `ICSR.PENDSVSET`. |
| `thunk_EXT_FUN_00002480` | `0x2480` | `0x60042c9c` | **`FUN_60042c9c`** -- **FreeRTOS `xEventGroupSetBits` (event_groups.c), reconstructed from a genuinely split/discontiguous function body -- confirmed by QA (see note).** Entry snippet at `0x60042c9c` validates the event group pointer and that `param_1` doesn't use the reserved top byte (`configASSERT`-style checks via `thunk_FUN_601016a2`, line numbers 0x203/0x204 -- i.e. lines 515/516 of the real `event_groups.c`), calls the increment-critical-nesting helper `FUN_60047958`, ORs the new bits into the group, then **falls/branches ~0x4500 bytes away to a loop physically located at flash 0x60047138-0x600471a4** (this is also `thunk_EXT_FUN_0000691c`'s target -- Ghidra's `getFunctionContaining()` correctly reports both ITCM offsets as the *same* Function object) that walks the list of tasks blocked on the group, testing each one's AND/OR wait condition and unblocking matches via `func_0x60047e08`, before clearing auto-clear bits and yielding through `tasks__60047aa8`. **Methodology note (QA-verified, session 6 follow-up):** the decompiled body on disk (`analysis/decomp/FUN_60042c9c.c`, 59 lines) already contains both regions as one coherent function -- confirmed independently by disassembling the raw bytes at `0x60047138` by hand, which begin `2de9 f041` = Thumb-2 `push.w {r4-r8,lr}`, a genuine function-body prologue-shaped instruction sequence with real branching logic (matching the `LAB_60047172`-`LAB_600471a4` labels in the decompile), not a stub. This really is one function's logic spread across two non-adjacent regions of the ITCM blob. **This is different from the `heap_5_improved__*` case below** -- see that note for why "split body" turned out to be the wrong diagnosis there; the batch-decompile output-file-naming collision that motivated re-running queries at canonical entry addresses did happen, but for a different underlying reason than this function's genuine split. |
| `thunk_EXT_FUN_00006820` | `0x6820` | `0x6004703c` | **`FUN_6004703c`** -- **FreeRTOS event-bits helper, sibling of `FUN_60042c9c`'s `xEventGroupSetBits`** -- this one matches `xEventGroupClearBits`/`xEventGroupWaitBits`'s inner "test and optionally clear" logic: validates the event-group pointer and bits parameter (`configASSERT`-style checks via `thunk_FUN_601016a2`, lines 0x131/0x132/0x133/0x136 of `event_groups.c`), tests `param_2` against the current bits either as "any" or "all" (`param_4` selects AND vs OR semantics), optionally clears the matched bits, and if the caller wants to block, calls `tasks__60047ce4` (add-to-blocked-list, by name pattern) then yields via `tasks__60047aa8`, waking via the `0x10000000`/PENDSVSET kick and re-checking the bits after the wake. |
| `thunk_EXT_FUN_0000691c` | `0x691c` | `0x60047138` | same function as above (**`FUN_60042c9c`**) -- additional veneer/call site into a different fragment of its body. |
| `thunk_EXT_FUN_000069a0` | `0x69a0` | `0x600471bc` | **`queue__600471bc`** -- `src: queue.c`. Guarded accessor calling the assert/panic primitive `thunk_FUN_601016a2` on an invariant check -- one of `xQueueGenericReceive`'s/`xQueueGenericSend`'s internal validity checks. |
| `thunk_EXT_FUN_00006a20` | `0x6a20` | `0x6004723c` | **`queue__6004723c`** -- `src: queue.c`. Small helper calling `queue__600471bc`; part of the same queue-internals cluster. |
| `thunk_EXT_FUN_00006a74` | `0x6a74` | `0x60047290` | **`queue__60047290`** -- `src: queue.c`. **`xQueueGenericSend`/"give" or peek core** -- called throughout this session's resolved functions (I2C driver, state machine, event-group code, registry teardown) as the universal "post/peek a queue item" primitive; by far the most-referenced function this session (7+ distinct callers found). |
| `thunk_EXT_FUN_00006bc8` | `0x6bc8` | `0x600473e4` | **`queue__600473e4`** -- `src: queue.c`. Companion receive/peek variant to `queue__60047290`, used by the link-state-machine function `FUN_60042050`. |
| `thunk_EXT_FUN_00006c88` | `0x6c88` | `0x600474a4` | **`FUN_600474a4`** -- **Stream-buffer/semaphore "give" primitive**: validates the buffer object (`configASSERT`-shaped checks, lines 0x43e/0x442/0x447), calls `FUN_60048628` (the NVIC-interrupt-priority assert -- see below, confirming this is reachable from ISR context), raises `BASEPRI` to mask normal-priority interrupts, and if there's room (`param_1[0xe] < param_1[0xf]`) increments the count and, if a receiver is already waiting (byte at `+0x45`), removes it from the blocked-on-receive list via `tasks__60047d80` and signals the caller. Matches FreeRTOS `xStreamBufferSendFromISR`/`xQueueGiveFromISR`'s shape (ISR-safe give with a matching wake). |
| `thunk_EXT_FUN_00006d2c` | `0x6d2c` | `0x60047548` | **`FUN_60047548`** -- **`xQueueGenericReceive` (FreeRTOS `queue.c`), not yet file-attributed by name but unmistakable in shape and confirmed by embedded `configASSERT` line numbers 0x4de/0x4e2/0x4e7 (lines 1246/1250/1255 of `queue.c`).** Validates the queue handle and item-size/buffer-pointer consistency, checks `xTaskGetSchedulerState()`-equivalent (`FUN_60047f50`), then loops: if an item is available, copies it out (`func_0x6004bbfe`), decrements the count, and if a blocked sender exists (`tasks__60047d80`) posts the PENDSVSET kick; otherwise, if `ticksToWait!=0`, blocks on the queue's receive-wait list (`tasks__60047ea8`/`tasks__60047cac`) and yields (`tasks__60047aa8`), retrying on wake. Sits directly beside its already-attributed siblings `queue__60047290` etc. |
| `thunk_EXT_FUN_00006e5c` | `0x6e5c` | `0x60047678` | **`queue__60047678`** -- `src: queue.c`. **`uxQueueMessagesWaiting()`/queue-count-and-take** core -- called from the registry lookup (`FUN_60046ffc`), the I2C-pending check (`FUN_6004bce8`), and the critical-section-guarded receive path (`FUN_60047548`). |
| `thunk_EXT_FUN_00007018` | `0x7018` | `0x60047834` | **`queue__60047834`** -- `src: queue.c`. Small queue-teardown helper calling `heap_5_improved__600488f4` (free) -- releases a queue's storage area. |
| `thunk_EXT_FUN_000070b0` | `0x70b0` | `0x600478cc` | **`FUN_600478cc`** -- List-accounting helper for a priority/ready-list structure: removes a list item (`FUN_6004bb60`) and, if the list becomes empty, clears the corresponding ready-priority bit in a bitmap (`*DAT_60047944 &= ~(1<<prio)`); otherwise re-inserts the item at a new sort key (`FUN_6004bb32`) computed from an accumulated value, with an overflow (`CARRY4`) path taking a different insertion helper. Shape matches FreeRTOS `tasks.c`'s delayed-task-list / ready-list re-insertion logic (`prvAddTaskToReadyList`-adjacent bookkeeping). |
| `thunk_EXT_FUN_0000713c` | `0x713c` | `0x60047958` | **`FUN_60047958`** -- Trivial global counter increment, `*DAT_60047964 += 1` -- by its position as the first call in nearly every FreeRTOS primitive resolved this session (`xEventGroupSetBits`, `xQueueGenericReceive`, `queue.c`/`tasks.c` internals all call it first), this is almost certainly **`vTaskSuspendAll()`** or the scheduler-suspend-count increment that FreeRTOS uses instead of a full critical section for list-manipulation-safe (but ISR-unsafe) regions. |
| `thunk_EXT_FUN_0000728c` | `0x728c` | `0x60047aa8` | **`tasks__60047aa8`** -- `src: tasks.c`. **The universal "yield / re-schedule now" call** -- invoked at the tail of nearly every blocking primitive resolved this session (`xQueueGenericReceive`, `xEventGroupSetBits`, the registry-drain helper, the hybrid-lock release); almost certainly `taskYIELD_IF_USING_PREEMPTION()`/`vTaskSwitchContext`-adjacent glue, distinct from but complementary to the raw scheduler core `FUN_60047be8`. |
| `thunk_EXT_FUN_000073cc` | `0x73cc` | `0x60047be8` | **`FUN_60047be8`** -- **FreeRTOS scheduler tick/switch core (`vTaskSwitchContext`-shaped).** Bracket-guarded by a re-entrancy flag (`*_DAT_60047c88`); on the real path it refreshes an "overflow count" bookkeeping value, calls the tick-delta helper `FUN_60041c50`, adjusts an accumulator if the tick moved backward, and then does a **`31 - CLZ(ready_priority_bitmap)`-style highest-ready-priority lookup** (`LZCOUNT` intrinsic) into a per-priority ready-list array (`_DAT_60047c9c + prio*0x14`), rotates that list's head via `FUN_6004904a`/pointer rewrite, and republishes the new current-TCB pointer plus its stack-limit field at `_DAT_60047ca8`. This is the routine `FUN_6004166c` (PendSV handler)'s `func_0x60047be8(0x50)` call invokes -- the actual "pick the next task to run" scheduler core. |
| `thunk_EXT_FUN_00007564` | `0x7564` | `0x60047d80` | **`tasks__60047d80`** -- `src: tasks.c`. Removes a task from a wait/blocked list (called with a list-item field as its argument) and reports whether a removal happened -- the "was someone waiting? un-block them" check used by both `xQueueGenericReceive` (`FUN_60047548`) and the ISR-safe stream-give primitive (`FUN_600474a4`). |
| `thunk_EXT_FUN_000075ec` | `0x75ec` | `0x60047e08` | **`FUN_60047e08`** -- **Event-group per-task unblock (`xEventGroupSetBits`'s inner "remove matched waiter" step)** -- called from `FUN_60042c9c`'s loop. Marks a list item's high bit, removes it from both the event-group's wait list and the task's generic item list (`FUN_6004bb60` x2), sets the task's ready-priority bit, re-inserts it into the ready list (`FUN_6004bb1a`), and updates a "higher-priority task became ready" flag if the woken task outranks the currently-running one -- classic `configASSERT`-guarded (line 0xd0f/0xd17) FreeRTOS unblock-and-maybe-preempt logic. |
| `thunk_EXT_FUN_00007734` | `0x7734` | `0x60047f50` | **`FUN_60047f50`** -- **`xTaskGetSchedulerState()`-equivalent**: returns 1 if the scheduler hasn't started (`*DAT_60047f68==0`), 2 if running, 0 if suspended (`*DAT_60047f6c==0`) -- the scheduler-state check every queue/event-group primitive above calls before deciding whether blocking is legal. |
| `thunk_EXT_FUN_000078b0` | `0x78b0` | `0x600480cc` | **`FUN_600480cc`** -- Reference-counted "pending ready" gate around a linked notify: under critical-section brackets (`FUN_60048580`/`FUN_600485c8`), if a counter at `obj+0xc4` is zero, sets a busy flag and (if a target is given) notifies it (`FUN_600478cc`) plus a PENDSVSET kick; then decrements/increments the counter depending on a direction flag. Generic FreeRTOS-adjacent notification-gate helper. |
| `thunk_EXT_FUN_00007d10` | `0x7d10` | `0x6004852c` | **`FUN_6004852c`** -- Queue-empty-check-then-log helper: calls `queue__60047290` in peek mode, and if the queue isn't actually empty (return != 1), formats and emits a diagnostic message via `func_0x6004c104`/`func_0x6004be44` -- an assertion-style consistency check ("this queue should be drained here") wrapping teardown code, called from both the flash-buffer-flush function (`FUN_60043608`) and the I2C driver (`FUN_600426c0`). |
| `thunk_EXT_FUN_00007d64` | `0x7d64` | `0x60048580` | **`FUN_60048580`** -- **`portENTER_CRITICAL()` / `vTaskEnterCritical` (nesting nesting-counter half).** Raises `BASEPRI` to `0x50`, increments a nesting counter at `*DAT_600485b8`, and on the *first* nesting level, asserts (line 0x193) that the scheduler is actually running if a companion flag is set -- textbook FreeRTOS critical-section-enter with the classic "don't call this before `vTaskStartScheduler` unless you know what you're doing" guard. Has 14 callers across this session's functions, confirming it's the central critical-section primitive. |
| `thunk_EXT_FUN_00007dac` | `0x7dac` | `0x600485c8` | **`FUN_600485c8`** -- **`portEXIT_CRITICAL()` (nesting-counter half, pairs with `FUN_60048580`).** Decrements the nesting counter and, once it reaches zero, drops `BASEPRI` back to 0 via `setBasePriority(0)` -- but only if actually in privileged/handler mode. Asserts (line 0x19a) if the counter was already zero on entry (unbalanced enter/exit). |
| `thunk_EXT_FUN_00007ddc` | `0x7ddc` | `0x600485f8` | **`FUN_600485f8`** -- Tick-hook-shaped wrapper: raises `BASEPRI`, calls `tasks__60047974` (a `tasks.c`-attributed function not otherwise decompiled this session), and if it signals a context-switch is needed, sets the PENDSVSET bit -- matches the shape of FreeRTOS's tick-timer ISR calling `xTaskIncrementTick()` and requesting PendSV on its return value. |
| `thunk_EXT_FUN_00007e0c` | `0x7e0c` | `0x60048628` | **`FUN_60048628`** -- **`portASSERT_IF_INTERRUPT_PRIORITY_INVALID()` -- the closest thing to direct NVIC interaction found this session.** Reads the current exception number (`getCurrentExceptionNumber() & 0x1f`, i.e. `__get_IPSR()`), and for external interrupts (`>0xf`) compares the NVIC's stored priority for that IRQ (byte array at `DAT_6004866c`, i.e. `NVIC->IP[]`/`SCB->SHP[]`) against `configMAX_SYSCALL_INTERRUPT_PRIORITY`, asserting (line 0x2e8) if a FreeRTOS API is being called from an interrupt with too-high priority to safely mask; a second check (line 0x2f8) asserts the current `BASEPRI` is within the syscall-priority range. This is a **compile-time-enabled sanity check**, not a live interrupt handler, but it's the only place this batch touched anything NVIC-shaped. |
| `thunk_EXT_FUN_00007f58` | `0x7f58` | `0x60048774` | **`heap_5_improved__60048774`** -- (Re-decompiled to its full 210-byte canonical body after an earlier batch run's query collision temporarily clobbered the output file with a 4-byte fragment -- see corrected methodology note under "additional veneer" rows below; this is *not* the same phenomenon as `FUN_60042c9c`'s genuine split body.) Already `src: heap_5_improved.c`-attributed; core `pvPortMalloc` free-list search. |
| `thunk_EXT_FUN_000080d8` | `0x80d8` | `0x600488f4` | **`heap_5_improved__600488f4`** -- (Same re-decompile fix as above; restored to its full 136-byte canonical body.) Already `src: heap_5_improved.c`-attributed; `vPortFree`-adjacent free-list insertion/coalescing. |
| `thunk_EXT_FUN_00008802` | `0x8802` | `0x6004901e` | **`FUN_6004901e`** -- ISR-context critical-section variant of `FUN_60048580`/`FUN_600485c8`'s pair: if called from handler mode (`getCurrentExceptionNumber()!=0`), saves/restores `BASEPRI` directly (`portSET_INTERRUPT_MASK_FROM_ISR`/`portCLEAR_INTERRUPT_MASK_FROM_ISR`-shaped) instead of going through the nesting counter; if called from thread mode, falls through to the plain `FUN_60048580` critical-section-enter. |
| `thunk_EXT_FUN_00008832` | `0x8832` | `0x6004904e` | resolves to **`heap_5_improved__60048774`**, but **not via a body fragment -- QA correction, see methodology note below.** `0x6004904e` is a bare 4-byte Thumb-2 `B.W 0x60048774` instruction (manually disassembled: bytes `ff f7 91 bb`), i.e. an unconditional tail-branch stub, not a continuation of the function's logic. |
| `thunk_EXT_FUN_00008844` | `0x8844` | `0x60049060` | resolves to **`heap_5_improved__600488f4`**, same correction as above: `0x60049060` is a bare 4-byte `B.W 0x600488f4` (bytes `ff f7 48 bc`), an unconditional branch stub sitting in the small gap right after the unrelated function `FUN_6004901e` (which ends at `0x60049044`), not a body fragment. |
| `thunk_EXT_FUN_0000b15c` | `0xb15c` | `0x6004b978` | **`FUN_6004b978`** -- Drains a linked list under the exclusive-monitor guard above (`FUN_6004b93e`): pops each node and re-queues it via `queue__60047290`, bracketed by the scheduler-suspend counter (`FUN_60047958`) and a yield (`tasks__60047aa8`) -- looks like flushing a deferred/pending-notification list into the real RTOS queue once the guard confirms it's safe to touch RTOS primitives. |
| `thunk_EXT_FUN_0000b2e8` | `0xb2e8` | `0x6004bb04` | **`FUN_6004bb04`** -- List-head initializer: sets up a circular sentinel node (`next=self+2`, `prev=self+2`, an `xItemValue`-style sentinel field to `0xffffffff`, count to 0) -- matches FreeRTOS `list.c`'s `vListInitialise()` exactly. |
| `thunk_EXT_FUN_0000b2fe` | `0xb2fe` | `0x6004bb1a` | **`FUN_6004bb1a`** -- Doubly-linked **list insert** (`vListInsert`/`vListInsertEnd`-shaped): splices a new item in after a given position, updates the neighbor's back-link, stores the owning list pointer in the item, and increments the list's count -- FreeRTOS `list.c` primitive, heavily reused (10 callers) across the `tasks.c`/`queue.c`/event-group code resolved this session. |
| `thunk_EXT_FUN_0000b316` | `0xb316` | `0x6004bb32` | **`FUN_6004bb32`** -- Doubly-linked **sorted list insert** (`vListInsert`, value-ordered variant): walks from a given start node while the next node's sort key is `<=` the new item's key, then splices in before that point -- the ready-list/delayed-list ordered-insertion counterpart to the unordered `FUN_6004bb1a`. |
| `thunk_EXT_FUN_0000b344` | `0xb344` | `0x6004bb60` | **`FUN_6004bb60`** -- Doubly-linked **list remove** (`uxListRemove`-shaped): unlinks an item from its neighbors, fixes up the owning list's "index" pointer if it pointed at the removed item, clears the item's owning-list field, decrements the count, and returns the new count -- FreeRTOS `list.c` primitive. |
| `thunk_EXT_FUN_0000b3e2` | `0xb3e2` | `0x6004bbfe` | **`FUN_6004bbfe`** -- Byte-copy-with-wraparound: copies `param_1[0x10]` bytes from a circular-buffer read cursor (`param_1[3]`, wrapping at `param_1[1]`) into the caller's output buffer -- `queue.c`'s item-dequeue copy-out helper (the actual `memcpy`-out-of-the-queue-storage-area step inside `xQueueGenericReceive`, called by `FUN_60047548` above). |
| `thunk_EXT_FUN_0000b4c2` | `0xb4c2` | `0x6004bcde` | **`FUN_6004bcde`** -- Second `xQueueGenericReceive`-shaped function, structurally identical to `FUN_60047548` but operating on a queue-set/queue-registry record at `param_1+0x50` (`configASSERT` lines 0x570/0x574/0x579/0x5c1 -- a different region of `queue.c`, consistent with this being the **queue-*set*** receive variant, `xQueueSelectFromSet`/`xQueueGenericReceive` called through a queue-set wrapper) -- includes an extra `tasks__600ca9e8` call on one path not present in the plain version. |
| `thunk_EXT_FUN_0000b4cc` | `0xb4cc` | `0x6004bce8` | **`FUN_6004bce8`** -- One-line queue-empty check: `return queue__60047678(handle) == 1;` -- `uxQueueMessagesWaiting()`-adjacent helper, called from the I2C driver to check for pending work. |
| `thunk_EXT_FUN_0000b52a` | `0xb52a` | `0x6004bd46` | resolves to **`heap_5_improved__600488f4`** via a **two-hop branch-thunk chain**, not a body fragment (QA correction): `0x6004bd46` is a bare `B.W 0x60049060` (bytes `fd f7 8b b9`), which is itself the stub described above that branches on to `0x600488f4`. It sits in the same small cluster of tiny memory-primitive functions as `memcmp`/`memcpy`/`memmove`/`memset`/`operator new` a few bytes away (`0x6004bd26`-`0x6004bde6`), consistent with being a standalone stub rather than part of a 14 KB-distant function's body. |
| `thunk_EXT_FUN_0000b52e` | `0xb52e` | `0x6004bd4a` | resolves to **`heap_5_improved__600488f4`** via a **three-hop branch-thunk chain** (QA correction): `0x6004bd4a` is a bare `B.W 0x6004bd46` (bytes `ff f7 fc bf`) -> `0x6004bd46` (`B.W 0x60049060`) -> `0x60049060` (`B.W 0x600488f4`) -> real function entry. Same cluster-of-stubs situation as the row above. |

**QA methodology note on the `heap_5_improved__*` rows above (corrects the original session-6 write-up):** the original write-up described `thunk_EXT_FUN_00008832`/`00008844`/`0000b52a`/`0000b52e` as targeting "different fragments" of `heap_5_improved__60048774`'s/`heap_5_improved__600488f4`'s body, using the same "split/discontiguous function" framing as `FUN_60042c9c`. A QA pass manually disassembled the raw bytes at each of the four target addresses (Thumb-2 `B.W`/`BL` decode done by hand, not via Ghidra, since this task couldn't run `analyzeHeadless`) and found every one of them is a **bare 4-byte unconditional branch instruction and nothing else** -- `0x6004904e`/`0x60049060`/`0x6004bd46` each contain a single `B.W` to the next hop in the chain, terminating at the real function entry (`0x60048774` or `0x600488f4`). This is architecturally the same veneer/thunk mechanism already described earlier in this document for the external flash-veneer table (`0x6013cee8` etc.) -- just implemented as bare in-blob `B.W` chains instead of the `LDR PC,[PC,#0]`/`LDR r0,[pc,#N];BX r0` idiom, and chained multiple hops deep in two of the four cases. **There is no "split body" here** -- the two `heap_5_improved__*` `.c` files on disk (`analysis/decomp/heap_5_improved__60048774.c`, 104 lines; `analysis/decomp/heap_5_improved__600488f4.c`, 52 lines) are each a single, complete, contiguous function, and both read as coherent, non-truncated logic (`pvPortMalloc` free-list search / `vPortFree`-adjacent coalescing respectively) on inspection. The original batch-decompile file-naming collision that prompted re-running these two at their canonical entry addresses was real, but its cause was Ghidra resolving these bare-branch stub addresses to (or through) the same target function rather than the target's body being physically discontiguous -- `FUN_60042c9c` (previous row group) is confirmed to be the only genuine split-body case found this session.

### FlexSPI / QSPI flash driver (LUT programming, cache maintenance, chunked flash write) (4 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00000348` | `0x348` | `0x60040b64` | **`FUN_60040b64`** -- Retry-loop wrapper around `FUN_6004083c` (a FlexSPI/QSPI status poll returning a 64-bit value); on success copies a run of words from a caller buffer into an offset-indexed region and toggles a state field `param_1+0x1c` between 2 and 1 around the copy -- shape matches a FlexSPI command-queue "stage data, kick, mark done" wrapper. |
| `thunk_EXT_FUN_000009c0` | `0x9c0` | `0x600411dc` | **`FUN_600411dc`** -- **FlexSPI LUT (Look-Up Table) programmer.** Classic NXP SDK `FLEXSPI_UpdateLUT()` shape: disables IRQs, clears LUT-key/enable bits at `+0x14`/`+0x250`/`+0x84`, writes each 32-bit LUT sequencer word to `+0x274` in a loop (computed from a packed `(count, index)` field at `+0x80`), then re-locks and repeats a mirrored write to `+0x260`. Reprograms the QSPI command sequencer that drives the W25Q128JW flash -- i.e. this (and its neighbors below) is the runtime FlexSPI/QSPI driver, distinct from the boot-time FCB. |
| `thunk_EXT_FUN_00000b1c` | `0xb1c` | `0x60041338` | **`FUN_60041338`** -- **D-cache clean+invalidate wrapped flash write helper.** Aligns a flash offset/length to 0x20 (Cortex-M7 cache-line size), writes the aligned range to `iVar4+0x268` in 0x20-byte strides (cache **clean-by-address**), calls `func_0x6004111c/func_0x60041090/func_0x60040cbc/func_0x60041164` (looks like the actual FlexSPI IP transfer-kick + wait-done sequence), then does the mirrored loop against `iVar4+0x25c` (cache **invalidate-by-address**) and clears a completion flag at `+0x250`. `param_1 + 0x60000000` is the QSPI-mapped flash address (matches CLAUDE.md's flash base) -- this is the runtime flash-program/erase driver's cache-maintenance shell. |
| `thunk_EXT_FUN_00000c7c` | `0xc7c` | `0x60041498` | **`FUN_60041498`** -- **Chunked flash write loop.** Same cache-clean/invalidate bracket as `FUN_60041338`, but internally loops calling `func_0x60041430(addr, buf, len)` for successive page-aligned chunks (chunk size computed from a page-size field at `iVar2+0x10`, minus the misalignment of the current address) -- this is the actual multi-page program routine sitting under the single-shot helper above; `func_0x60041430` is presumably the single-page FlexSPI IP write primitive. |

### IOMUXC_GPR (0x400AC000) pin-mux bitfield helpers (3 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_000007b8` | `0x7b8` | `0x60040fd4` | **`FUN_60040fd4`** -- Generic **bitfield READ** helper for a register block at `_DAT_60041024` (paired with the WRITE helper `FUN_60041028` below, which targets `0x400ac000` = IOMUXC_GPR): decodes a packed `(reg_index, shift, width)` descriptor from `param_1`, brackets the read in `func_0x6004bf6c`/`func_0x6004c2f4` (lock/unlock-style) and an IRQ-disable critical section, returns the extracted field. |
| `thunk_EXT_FUN_0000080c` | `0x80c` | `0x60041028` | **`FUN_60041028`** -- Generic **bitfield WRITE** helper, read-modify-write on `*(uint*)(((param_1&0xffffff)>>0x10)*4 + 0x400ac000)` -- **0x400AC000 is IOMUXC_GPR** on i.MX RT1060/1062. Same lock-bracket/IRQ-disable shape as `FUN_60040fd4`. This is the low-level primitive the NXP SDK's `IOMUXC_GPR_...` pin/mux/routing macros compile down to; called by `FUN_6004244c` (see below) to steer a mux. |
| `thunk_EXT_FUN_00001c30` | `0x1c30` | `0x6004244c` | **`FUN_6004244c`** -- Pin/mode-select routine sitting directly on top of the IOMUXC_GPR bitfield writer: reads a mode byte, on a one-time-armed condition logs via `func_0x6004c304`, then for mode values 0/1/2 calls `FUN_60041028` (the 0x400AC000 IOMUXC_GPR writer) twice with different `(reg,value)` pairs to steer a mux, followed by another log call and a small event-record append (`func_0x6004c3f4`/`func_0x6004c0c4`/`func_0x6004c12c`) before dispatching through `func_0x60048b0c` + `func_0x600491b2` (the error-code translator, see below). Reads like a runtime pin-routing/mode-select entry point (e.g. selecting which peripheral owns a shared pin) rather than a raw GPIO bit toggle. |

### CCM (0x400FC000) clock-gate/mux bitfield helpers with CDHIPR handshake wait (3 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00000d84` | `0xd84` | `0x600415a0` | **`FUN_600415a0`** -- Bitfield read-modify-write on a fixed peripheral base `&DAT_400fc000` (**0x400FC000 = CCM**, the i.MX RT1060 Clock Controller Module) -- decodes `(index, shift, width)` from `param_1` like the IOMUXC helpers above, then busy-waits on bit `param_1>>0x1a` of `[base+0x48]` unless that field is 0x20. `[CCM+0x48]` is **CDHIPR** (Clock Divider Handshake In-Process Register) -- this is the SDK's `CLOCK_SetDiv`/`CLOCK_SetMux`-style helper that must wait for the hardware handshake after changing a clock mux/divider that feeds an active peripheral. |
| `thunk_EXT_FUN_00000dc4` | `0xdc4` | `0x600415e0` | **`FUN_600415e0`** -- Byte-identical sibling of `FUN_600415a0` (same CCM+CDHIPR bitfield-write-then-handshake-wait shape), just a different call site / inlined copy. |
| `thunk_EXT_FUN_00000e04` | `0xe04` | `0x60041620` | **`FUN_60041620`** -- Third byte-identical sibling of `FUN_600415a0`/`FUN_600415e0` -- same CCM clock-mux/div bitfield-write + CDHIPR handshake wait. |

### Standalone D-cache clean-by-address helpers (2 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00000f80` | `0xf80` | `0x6004179c` | **`FUN_6004179c`** -- Cache-clean-by-address loop targeting `[_DAT_600417c4 + 0x25c]` in 0x20-byte (cache-line) strides, bracketed by DSB/ISB -- a standalone cache-maintenance primitive, same shape as the write-side half of `FUN_60041338`/`FUN_60041498`'s bracket but factored out on its own. |
| `thunk_EXT_FUN_00000fac` | `0xfac` | `0x600417c8` | **`FUN_600417c8`** -- Byte-identical sibling of `FUN_6004179c` against a different base register (`_DAT_600417f0`) -- same cache-clean-by-address loop, a second DMA/flash-adjacent client of the same primitive. |

### Embedded microphone/audio DSP pipeline (FFT, IIR/CIC filters, noise-suppression-style gain control, receiver.cc) (28 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00002dec` | `0x2dec` | `0x60043608` | **`FUN_60043608`** -- Buffer-flush routine referencing the literal strings **`"CHECK failed"`** and **`"append_buffer.h"`** (`PTR_s_CHECK_failed_60122ce9_9`, `PTR_s_append_buffer_h_60043698`) via a `CHECK`-style assert/log call (`func_0x6004c1f4`+`func_0x6004be44`) when a fill-level field underruns a threshold; once the buffer reaches 0x1e0 (480) bytes it copies out 0x3c0 bytes via `FUN_6004bd8e` (memcpy), optionally forwards them to a secondary sink `FUN_600498b2`, then ships the 0x1e0-byte block out through a vtable call. `append_buffer.h` is an idiomatic Google/WebRTC-style internal header name -- concrete string-level confirmation that this code is Google/WebRTC-derived, matching CLAUDE.md's note that `gotham` embeds a full WebRTC stack; here it shows up **inside `bruce` (the BT-only firmware) too**, as part of what this session's other findings (below) show is an on-device audio pipeline. |
| `thunk_EXT_FUN_00002fac` | `0x2fac` | `0x600437c8` | **`FUN_600437c8`** -- Per-sample clamp-and-quantize loop: clamps each of `param_2` input floats to `[fRam60043830_hi, fRam60043830_lo]`-style bounds and truncates to `short`, writing a 16-bit PCM output array -- a float-to-int16 PCM conversion/limiter stage, the tail end of a float DSP pipeline. |
| `thunk_EXT_FUN_0000316c` | `0x316c` | `0x60043988` | **`FUN_60043988`** -- Windowing function: multiplies the first 0x60 (96) and a separate `0xa1..0x100` (95-sample) tail of a float buffer element-wise against two fixed coefficient tables (`DAT_600439c4`/`DAT_600439c8`) -- classic asymmetric analysis-window application (e.g. a Hann/Vorbis-style window with a flat middle), consistent with an FFT-based audio codec's frame windowing stage. |
| `thunk_EXT_FUN_0000323c` | `0x323c` | `0x60043a58` | **`FUN_60043a58`** -- Per-bin spectral gain computation: for each of **129 (`0x81`) frequency bins** (loop bound is `param_1+0x14c0`, starting from `param_1+0x12bc` -- a `0x204`-byte / 129-float span, matching the `pfVar6[0x81]` index also used inside the loop body; **QA correction: the original write-up said "~0x480/4=0x120 (288)" bins, an arithmetic slip -- `0x14c0-0x12bc` is `0x204`, not `0x480`**), compares a "speech" vs "noise" energy pair, computes a smoothed ratio-based gain (classic **Wiener-filter-style noise-suppression gain formula**, `gain = f(prior_snr, post_snr)`), and writes the result plus an update to a smoothed-noise-energy state array -- textbook shape for a WebRTC-NS-style (or similar) single-band noise-suppression gain stage. 129 bins matches the FFT working size used throughout this pipeline (`FUN_60043af0`, `FUN_60044440`, `FUN_6004a0e6`, etc.). |
| `thunk_EXT_FUN_000032d4` | `0x32d4` | `0x60043af0` | **`FUN_60043af0`** -- **Multi-band adaptive gain/VAD-style controller.** Accumulates band energy over a 0x81 (129, i.e. an FFT-bin-count-shaped) loop into a running average, then computes three smoothed control signals via `FUN_6004898c` (a soft nonlinearity, e.g. tanh/sigmoid-shaped) from energy deltas against three reference bands, blends them into a single gain-trend value clamped to `<=1.0`, and finally derives a per-bin gain array (`1/(x*(1-g)/(g+1)+1)`) written out to `param_1+0x5818`. Shape (multi-band energy tracking -> blended control signal -> per-bin gain curve) matches an **automatic-gain-control / noise-suppression "gain computation" stage** of a WebRTC-style `audio_processing` pipeline; corroborates `FUN_60043a58` and the FFT/CIC functions below as one coherent subsystem. |
| `thunk_EXT_FUN_000034b8` | `0x34b8` | `0x60043cd4` | **`FUN_60043cd4`** -- Frame-completion tail: decrements a per-frame counter at `+0x18d0`; while positive, dispatches through an unresolved jump table (`_DAT_6004bea0`, "too many branches"); once it reaches 0, resets the counter to 500 and folds an accumulated energy value (`+0x2314`) into a running average (`+0x2310`) scaled by `fRam60043d34`. 500-frame averaging window at what's presumably a per-10ms-frame cadence (~5s) -- looks like a slow-moving background-noise/level estimate update, another AGC/NS-adjacent piece. |
| `thunk_EXT_FUN_0000351c` | `0x351c` | `0x60043d38` | **`FUN_60043d38`** -- Per-bin blend/limiter: for each of **129 (`0x81`) bins** (`0x1cf4-0x1af0` = `0x204` bytes = 129 floats; **QA correction: the original write-up computed this span as "~0x120 (288)," a hex-subtraction error -- `0x1cf4-0x1af0` is `0x204`, not `0x480`**), blends two candidate output values via a smoothed mix factor (`fVar8`) and applies a floor when the mix factor drops below a threshold, taking the larger of the current vs. previous-frame blended value when they diverge -- another per-bin spectral post-processing stage in the same 129-bin pipeline as `FUN_60043a58`/`FUN_60043af0`. |
| `thunk_EXT_FUN_00003c24` | `0x3c24` | `0x60044440` | **`FUN_60044440`** -- **Per-bin adaptive step-size gain smoother with periodic snapshot/history rotation.** Runs an FFT-domain gain-tracking loop (0x81/129 bins again) with an asymmetric attack/release step (`fVar1`/`0.25` up vs `0.75` down, classic AGC-style asymmetric smoothing) converging each bin toward a target derived from `VectorSignedToFloat`-converted history samples, and every 200 frames (a history-index field capped at 199/200) snapshots the whole 0x81-bin buffer out via `thunk_FUN_600dd6a0` -- consistent with a noise-floor/background-spectrum tracker that periodically commits its running estimate. |
| `thunk_EXT_FUN_00003db8` | `0x3db8` | `0x600445d4` | **`FUN_600445d4`** -- **Fixed-point cascaded multi-stage recursive filter over a 129-word (`0x2000`-scaled) working buffer**, iterated in several distinct stages each processing groups of 0xf0/0x3c0/0x780/0xa0/0x500-sized runs with 3-coefficient IIR-style recurrences (constants `0xbea`, `0x2498`, `0x3ad7`, `0x335`, `0x17de`, `0x305e` reused across stages) followed by an interleave/de-interleave (bit-reversal-shaped) reorder and a final per-tap FIR combine against two coefficient tables (`DAT_60044950`/`DAT_60044954`) before clamping to int16. Shape (cascaded fixed-point recursive stages + final FIR + clamp) matches a **CIC-style decimation/interpolation filter bank**, most plausibly the front-end that converts oversampled PDM/sigma-delta microphone samples down to the 129-bin working rate the float FFT/NS pipeline above operates on. |
| `thunk_EXT_FUN_00004a3c` | `0x4a3c` | `0x60045258` | **`FUN_60045258`** -- In-place complex-conjugate-symmetric spectral operation over a 0x3e (62)-element window: for each index pair walking inward from both ends of the buffer with coefficient table `_DAT_600452ec`, applies a 2x2 rotation-style combine (`a*c+b*s`, `-b*c+a*s`) matching a **real-FFT post-processing "unpack" step** (converting a packed real-input FFT's N/2 complex outputs into a full spectrum, or the inverse); negates the Nyquist-adjacent taps at both ends -- consistent with completing the Ooura-style real FFT used elsewhere in this pipeline (see `FUN_6004a0e6`). |
| `thunk_EXT_FUN_00004ad4` | `0x4ad4` | `0x600452f0` | **`FUN_600452f0`** -- **Bit-reversal / digit-reversal permutation for a radix-4 FFT**, operating on 4 interleaved 8-byte-stride sub-arrays (offsets `+0x20`,`+0x40`,`+0x60`,`+0x64` relative to a swapped pair) driven by a base-4 index table `DAT_60045438`; classic FFT input/output reorder stage. |
| `thunk_EXT_FUN_00004ca8` | `0x4ca8` | `0x600454c4` | **`FUN_600454c4`** -- Tiny fixed-point-to-float convert-and-scale: `(A + VectorUnsignedToFloat(x)*B) * C` from 3 float constants -- a small calibration/scale helper feeding `FUN_60043af0`'s energy accumulation. |
| `thunk_EXT_FUN_00004cd0` | `0x4cd0` | `0x600454ec` | **`FUN_600454ec`** -- Float-to-fixed quantizer with soft clamp: scales an input by two constants, clamps to a floor, applies an int-round-trip smoothing correction, then combines with 4 more float constants into a final `VectorFloatToUnsigned` conversion -- another small fixed<->float glue helper in the audio pipeline. |
| `thunk_EXT_FUN_00005614` | `0x5614` | `0x60045e30` | **`FUN_60045e30`** -- Straight-line sum of 15 floats starting from an accumulator seed (`fRam60045e48`) -- energy/sum accumulator, likely a sub-band energy total feeding one of the gain-control functions above. |
| `thunk_EXT_FUN_00005ab4` | `0x5ab4` | `0x600462d0` | **`FUN_600462d0`** -- Windowed-overlap staging into a 129-float scratch buffer (`afStack_220`): mode 0 forwards straight to `thunk_FUN_600dcd1c` (an FFT-adjacent thunk, see `FUN_6004a0e6`'s callees) twice for two half-frames; mode 2 instead applies a window-coefficient table (`_DAT_6004636c`) element-wise to both halves before combining -- classic "windowed overlap-add framing before FFT" front end, then hands off to `FUN_6004aee0`. |
| `thunk_EXT_FUN_0000645c` | `0x645c` | `0x60046c78` | **`FUN_60046c78`** -- Small mode-select shim: if a global session-state field's low bits are all zero, delegates to `func_0x6004970e` (mirrors `FUN_6004970e` below, a per-channel dispatch-via-jump-table function); otherwise checks a per-parameter connection/channel-index test via `func_0x6004c27c`. Routing glue between whatever registers channels (see `FUN_60046fa4`/`FUN_60046ffc` below) and the per-channel dispatcher. |
| `thunk_EXT_FUN_000064b0` | `0x64b0` | `0x60046ccc` | **`FUN_60046ccc`** -- **Microphone receive entry point, `receiver.cc`.** Confirmed via literal strings `"audio"`, `"ReceiverBuffer"`, `"receiver.cc"`, and the log message `"Pausing microphone receiving due ..."`. Dispatches on a per-object mode byte: mode 0 forwards a captured PCM chunk (size 0x780 = 1920 bytes) to `func_0x600428b8` for processing/encoding; mode 1 instead calls `func_0x6004beb4` (likely a silence/passthrough fill) on the same buffer; on a mode-0 processing failure it formats and emits the "Pausing microphone receiving due to <code>" log line and posts an event via `tasks__60047b98(0xfa)` (the PENDSVSET-context-switch-request primitive from session 5) -- i.e. this is exactly the code that decides to pause mic capture on an internal error and wakes whatever task handles that notification. **This, together with the FFT/CIC/gain-control cluster above, is the strongest evidence yet that `bruce` (the BT-only firmware) actively captures and DSP-processes microphone audio on-device** -- plausibly for the controller's Assistant-button mic, and structurally consistent with `gotham`'s known embedded WebRTC stack (CLAUDE.md) sharing its audio_processing module with `bruce`. |
| `thunk_EXT_FUN_00008f98` | `0x8f98` | `0x600497b4` | **`FUN_600497b4`** -- Converts an iterator-pair `{begin,end}` structure (matching `FUN_60049800`'s shape) into a `(ptr,count)` pair, feeds it through the float-to-int16 PCM quantizer `FUN_600437c8` into a stack buffer, and (unless a suppression flag at `+0x1b9` is set) forwards the resulting 0xa0-sample PCM block via `func_0x6004495c` -- the tail end of the audio pipeline packaging processed samples for output/transmission. |
| `thunk_EXT_FUN_0000912c` | `0x912c` | `0x60049948` | **`FUN_60049948`** -- Fixed-point sample-rate/format convert wrapper: calls `thunk_FUN_600dc512(...)` (an unresolved-target thunk, likely the actual resampler/format-converter) with a sentinel `0xffffffff` and, on success, writes back two converted 16-bit values -- glue around a lower-level (still-thunked) audio conversion primitive. |
| `thunk_EXT_FUN_0000942a` | `0x942a` | `0x60049c46` | **`FUN_60049c46`** -- Three-way FFT-stage dispatch: calls the FFT-adjacent thunk `thunk_FUN_600dcd1c` three times over different sub-ranges derived from an iterator-pair struct -- another piece of the same FFT front-end as `FUN_600462d0`. |
| `thunk_EXT_FUN_0000947c` | `0x947c` | `0x60049c98` | **`FUN_60049c98`** -- **Spectral magnitude/energy computation feeding the noise-suppression gain code.** For 0x7e (126) complex bin pairs, extracts real/imaginary parts, computes squared magnitude `re^2+im^2` and passes it through `FUN_6004a34c` (the fast approximate-sqrt-via-float-bit-trick below) to get magnitude, storing `{re, im, magnitude+1}` triples -- classic per-bin magnitude-and-phase-adjacent prep step before a Wiener-gain computation like `FUN_60043a58`. First calls `FUN_6004a0e6` (the FFT dispatcher) to actually transform the frame. |
| `thunk_EXT_FUN_0000954a` | `0x954a` | `0x60049d66` | **`FUN_60049d66`** -- **Complex radix-4/radix-2 FFT butterfly stage over a 16-element (128-point, split-radix-shaped) block, iterated across the full transform length.** Textbook decimation-in-time butterfly combine (sum/difference pairs, then twiddle-factor complex multiplies using `param_3` as the twiddle table) matching a hand-unrolled first stage of a classic split-radix real/complex FFT -- another concrete piece of the same Ooura-style FFT machinery identified in `FUN_6004a0e6`. |
| `thunk_EXT_FUN_000098ca` | `0x98ca` | `0x6004a0e6` | **`FUN_6004a0e6`** -- **Real-valued mixed-radix FFT dispatcher (Ooura-FFT-shaped).** Regenerates cosine/sine twiddle tables on demand (`thunk_FUN_6007d5fc`/`thunk_FUN_6007d578` when cached table sizes are stale), then for the forward-transform branch (`param_2<0`) does a butterfly combine of the DC/Nyquist pair, calls the actual radix-N butterfly passes (`thunk_FUN_600dd5a0`, `thunk_FUN_600dcd46`, and an unresolved jump-table call at `0x6004bf90`) or a base-case radix-4 transform (`thunk_FUN_600dd364` for `param_1==4`); the inverse-transform branch mirrors the same calls in reverse order. The 129-bin working size (`param_1` up to the caller's 0x100/0x81 buffers) and "regenerate twiddle table on size change" caching idiom matches the public-domain **Ooura FFT package that WebRTC vendors** for its `aec3`/audio-processing FFT needs -- strong corroboration this is WebRTC-derived code, not a bespoke implementation. |
| `thunk_EXT_FUN_00009afe` | `0x9afe` | `0x6004a31a` | **`FUN_6004a31a`** -- FFT post-processing chain: calls the bit-reversal permutation (`FUN_600452f0`), a radix-4 butterfly pass (`FUN_6004a27c`, not decompiled this session), the real-FFT spectrum-unpack step (`FUN_600451dc`, not decompiled this session), and finally combines the DC/Nyquist pair -- ties `FUN_600452f0` and `FUN_60045258`'s pieces together into the complete forward-FFT sequence, called from `FUN_6004aee0`. |
| `thunk_EXT_FUN_00009b30` | `0x9b30` | `0x6004a34c` | **`FUN_6004a34c`** -- **Fast approximate square-root via IEEE-754 bit manipulation**: `(x_bits + 0x3f800000) >> 1` (0x3f800000 = the float bit-pattern for 1.0) -- the classic "average the exponent with 1.0's exponent, halve the mantissa" fast-sqrt trick (a well-known relative of the "fast inverse square root"), used throughout the audio pipeline (`FUN_6004a4ac` and others) wherever a magnitude/energy value needs a cheap sqrt without a real FPU sqrt instruction round-trip. |
| `thunk_EXT_FUN_00009c90` | `0x9c90` | `0x6004a4ac` | **`FUN_6004a4ac`** -- Loop applying `FUN_6004a434` (not decompiled this session) to every 0x24-byte (36-byte) record in a `{begin,end}`-style array -- a per-record transform/update loop over a fixed-stride table, plausibly a per-band filter-state array in the same audio pipeline given the surrounding call graph (`FUN_60049a4a`). |
| `thunk_EXT_FUN_0000a0be` | `0xa0be` | `0x6004a8da` | **`FUN_6004a8da`** -- Scale-by-scalar loop over 0x41 (65) floats: `out[i] = in[i] * scale`, with the scale value substituted from a fallback field (`param_2+4`) when the caller passes a negative gain -- generic spectral-gain-application step (multiply a magnitude spectrum by a computed gain curve), consistent with applying the Wiener gains computed elsewhere in this pipeline. |
| `thunk_EXT_FUN_0000a94e` | `0xa94e` | `0x6004b16a` | **`FUN_6004b16a`** -- **5-coefficient Direct-Form-I IIR biquad filter**: `y = b0*x[n] + b1*x[n-1] + a1*y[n-1] - a2*y[n-2] - a3*y[n-3]`-shaped recurrence over a state vector of 9 floats (5 coefficients + 4 state), run per-sample over an output buffer -- a shelving/de-emphasis/DC-blocking filter stage, another piece of the audio DSP chain. |

### ARM exclusive-monitor (LDREX/STREX) spinlock / call-once primitives (6 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000ac80` | `0xac80` | `0x6004b49c` | **`FUN_6004b49c`** -- Guard-and-trap wrapper: calls the exclusive-monitor once/lock primitive `func_0x6004b9a8` (see `FUN_6004b9a8` below) and traps via `thunk_FUN_6010209a()` if it returns nonzero -- "this must succeed or we abort" pattern around static/lazy initialization. |
| `thunk_EXT_FUN_0000b122` | `0xb122` | `0x6004b93e` | **`FUN_6004b93e`** -- **ARM exclusive-monitor (LDREX/STREX) spinlock/once-guard, "acquire" half.** Uses `ExclusiveAccess`/`hasExclusiveAccess` to CAS a sentinel word from `-1` (uninitialized) to `-2` (initializing). **QA correction: the branch/return description below was backwards in the original write-up.** Reading the decompile (`analysis/decomp/FUN_6004b93e.c`) closely: if the sentinel is anything other than `-1` on first read (i.e. *not* uninitialized -- either mid-init or already done), the function skips the CAS attempt, spins on `thunk_FUN_6009257c()` while the value reads `-2`, and **returns 0 once it settles** -- this is the "someone else already handled it" / loser path, not the winner. If the caller instead *wins* the race (found `-1` and successfully CAS'd it to `-2`), the function falls through to an indirect call Ghidra could not resolve (`/* WARNING: Could not recover jumptable at 0x6004be94. Too many branches */`, `uVar2 = (*_DAT_6004be98)(); return uVar2;`) and returns *that* call's result, not a literal 0. Unlike its sibling `FUN_6004b9a8` (next row), `FUN_6004b93e` takes no callback parameter, so what that unresolved indirect call actually dispatches to is **not established** by this decompile -- flagged as open rather than asserted. Building block for lazy static-initialization guards usable **before** the RTOS scheduler exists (unlike the queue-based mutexes), but the exact winner-path behavior needs the jump table resolved (in Ghidra, not available to this QA pass) before it can be described with confidence. |
| `thunk_EXT_FUN_0000b18c` | `0xb18c` | `0x6004b9a8` | **`FUN_6004b9a8`** -- **Exclusive-monitor "call once" primitive**: same CAS-on-sentinel dance as `FUN_6004b93e` but the winner directly invokes a callback (`param_2`) before committing the sentinel to `2` (done); losers spin until the state leaves `1` (in-progress). Textbook `pthread_once`/C++11 `call_once`-equivalent for freestanding/pre-scheduler code, called from `FUN_6004b49c`'s guard-and-trap wrapper. |
| `thunk_EXT_FUN_0000b1c4` | `0xb1c4` | `0x6004b9e0` | **`FUN_6004b9e0`** -- Exclusive-monitor "try-acquire"/state-check variant: same `-1`/`-2` CAS dance as `FUN_6004b93e`, but returns `0` immediately if the sentinel was `-1` (uninitialized -- caller must init) rather than treating that as the success path; spins on `-2` (initializing) until settled, then returns `1`. Used by the `FUN_6004baa8`/`FUN_6004bab0` lock/unlock pair below to detect "is the real RTOS mutex ready yet". |
| `thunk_EXT_FUN_0000b28c` | `0xb28c` | `0x6004baa8` | **`FUN_6004baa8`** -- **Hybrid lock "acquire" (spinlock-or-RTOS-mutex).** Checks readiness via `FUN_6004b9e0`; if not ready, falls back to a pure spinlock path (`thunk_FUN_60092528` + `FUN_6004ba16`, both not decompiled this session) so locking works before the scheduler/queue subsystem is usable; if ready, still calls the spinlock helpers but into a local, then swaps the result into the real lock word -- a pre-scheduler-safe mutex acquire. |
| `thunk_EXT_FUN_0000b294` | `0xb294` | `0x6004bab0` | **`FUN_6004bab0`** -- **Hybrid lock "release", pairs with `FUN_6004baa8`.** If not yet RTOS-ready, releases via the low-level spinlock path (`thunk_FUN_60092528`); once ready, dispatches to either `queue__600c9e6c` (a not-yet-seen queue variant) or the standard `queue__60047290` "give" depending on a mode flag at `lock+8`, bracketed by the scheduler-suspend/yield pair (`FUN_60047958`/`tasks__60047aa8`) -- the release-side counterpart that lets this lock work both before and after the scheduler starts, exactly the primitive `FUN_600416d4`'s time-conversion code uses to protect its shared state. |

### C++ object teardown helpers (5 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000ac06` | `0xac06` | `0x6004b422` | **`FUN_6004b422`** -- Calls `func_0x6004b47a` (an `operator delete`-shaped free helper, see next row) on a field at `param_1+4`, then zero-fills the first 16 bytes of `param_1` -- object-teardown helper. |
| `thunk_EXT_FUN_0000ac1e` | `0xac1e` | `0x6004b43a` | **`FUN_6004b43a`** -- Iterates a fixed-size table (16-entry array of 16-byte records reached via `FUN_60046ed4`'s registry) calling `FUN_6004b422`'s teardown on each entry, then frees the table itself via `func_0x6004b47a` and clears three tracking fields -- bulk teardown of the registry `FUN_60046ed4` lazily allocates. |
| `thunk_EXT_FUN_0000b068` | `0xb068` | `0x6004b884` | **`FUN_6004b884`** -- Zero-fills 16 bytes starting at `param_1` -- plain zero-init helper (Ghidra flagged and removed an unreachable block at a stale address during decompilation, a benign analysis artifact, not a functional issue). |
| `thunk_EXT_FUN_0000b070` | `0xb070` | `0x6004b88c` | **`FUN_6004b88c`** -- Object teardown: frees a field (`FUN_6004b47a`), invokes a vtable destructor slot if present, then zero-fills via `FUN_6004b884` and returns `1` -- another link in the same C++-teardown chain as `FUN_6004b422`/`FUN_6004b43a`. |
| `thunk_EXT_FUN_0000b08e` | `0xb08e` | `0x6004b8aa` | **`FUN_6004b8aa`** -- One-line vtable call: `(*vtable[4])(); return 1;` -- generic virtual-dispatch teardown/close step. |

### operator delete-shaped deallocator (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000ac5e` | `0xac5e` | `0x6004b47a` | **`FUN_6004b47a`** -- **`operator delete`-shaped deallocator**: if the pointer is non-null, calls `FUN_6004b46e` (not decompiled this session, presumably a destructor-dispatch or bookkeeping step) on the 8-byte-adjusted block header, then frees via `func_0x6004c2bc` -- the free-side counterpart referenced by several of the C++-object-teardown helpers above. |

### operator new-shaped allocator with OOM-hook retry (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000b532` | `0xb532` | `0x6004bd4e` | **`FUN_6004bd4e`** -- **`operator new`-shaped allocator with retry-on-OOM-hook then abort.** Loops calling `heap_5_improved__60048774` (malloc); on failure, calls a registered new-handler-style callback (`thunk_FUN_600cc9c4`) and retries if one was registered, else traps via `thunk_FUN_6010209a()` -- textbook C++ `operator new`'s out-of-memory retry loop (minus C++ exceptions, which this freestanding target replaces with an abort). |

### Atomic intrusive refcount teardown (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00001680` | `0x1680` | `0x60041e9c` | **`FUN_60041e9c`** -- **Atomic (LDREX/STREX) reference-count decrement with dtor/free vtable calls.** Branches on a global mode flag (`_DAT_60041f10`): the fast path does a plain non-atomic decrement (pre-scheduler / uniprocessor-safe context), the slow path uses `ExclusiveAccess`/`hasExclusiveAccess` (ARM exclusive monitor) to decrement atomically; when refcount hits 1->0 it calls a vtable dtor (`obj[0]+8`) then a second refcount (weak-count) field, and when *that* also hits zero calls a second vtable slot (`obj[0]+0xc`, presumably `operator delete`). Classic C++ intrusive-refcounted-object teardown (`Release()`). |

### heap_5-shaped allocator internals (not yet file-attributed by name) (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00008836` | `0x8836` | `0x60049052` | **`FUN_60049052`** -- **`pvPortMalloc`-adjacent allocator internals, `heap_5_improved.c`-shaped (not yet file-attributed by name).** Two paths: for power-of-two-ish large requests (`param_1>8` and a power-of-two test) it does an aligned allocation directly via `heap_5_improved__60048774` with manual header patching; otherwise it's the classic heap_5 free-list search (`configASSERT`-guarded, lines 0xf3/0xf4/0x1bb) -- walks a size-sorted free list, splits the found block if there's more than 16 bytes of slack (`FUN_60048684`, presumably `prvInsertBlockIntoFreeList`), and on total exhaustion falls through to `heap_5_improved__600cc6a0`/`thunk_FUN_60052230`/`heap_support__60052254` (an out-of-memory hook chain, already-known `heap_support` module). |

### Generic keyed "get or create" registry / linked-list-by-key lookup (5 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_000066b8` | `0x66b8` | `0x60046ed4` | **`FUN_60046ed4`** -- Lazy-allocate-a-fixed-size(0x10c)-record-and-register helper: checks a keyed lookup (`func_0x60046f10(0)`), and if absent, allocates 0x10c bytes, zero-fills it (`FUN_6004bdd6`), and registers it via `func_0x6004c2e4` -- "get or create" pattern feeding the same registry as `FUN_60046fa4`/`FUN_60046ffc`. |
| `thunk_EXT_FUN_000066f4` | `0x66f4` | `0x60046f10` | **`FUN_60046f10`** -- Keyed accessor into a table looked up via `_DAT_60046f40`/`_DAT_60046f44` (guarded by `func_0x6004b49c`, an exclusive-monitor-backed once/lock primitive -- see `FUN_6004b49c` below): returns `table[param_1]` if the table pointer resolves via `FUN_60046ffc`, else 0. |
| `thunk_EXT_FUN_00006788` | `0x6788` | `0x60046fa4` | **`FUN_60046fa4`** -- "Get or create" channel/stream registration: checks a channel-active flag (`FUN_60047f34`), and if unset, allocates a 0x34-byte node from the heap (`heap_5_improved__60048774`), zero-initializes its first 0x14 bytes, copies a config pointer field from a global, and links it in via `FUN_60047f1c`. Same family as `FUN_60046ed4`; the 0x34-byte node size and adjacency to `FUN_60046ccc`'s mic-receiver code suggests this is the per-channel/per-stream registration for the audio pipeline (e.g. registering a new microphone/receiver channel). |
| `thunk_EXT_FUN_000067e0` | `0x67e0` | `0x60046ffc` | **`FUN_60046ffc`** -- Linked-list-by-key lookup: iterates a list rooted at `iVar2+0x2c` (obtained from `FUN_60046fa4`) comparing each node's key field against `param_1`, guarded by `queue__60047678`/`queue__60047290` (RTOS-queue-based mutex take/give) -- the lookup half of the get-or-create registry pattern above. |
| `thunk_EXT_FUN_00008ef2` | `0x8ef2` | `0x6004970e` | **`FUN_6004970e`** -- Per-channel dispatcher: fetches a channel handle from `param_1+0x58` (asserting, line 0x136b, if unset), enters a critical section (`FUN_60048580`), marks the channel's state byte `2`, then dispatches through an unresolved ("too many branches") byte-indexed jump table at `0x60048172`. Companion to `FUN_60046c78`'s channel-state check. |

### std::optional-style tagged-union merge helpers (2 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000ab28` | `0xab28` | `0x6004b344` | **`FUN_6004b344`** -- "Copy-if-valid" merge for a tagged-union-shaped `{value, bool}` pair: copies `param_2`'s value into `param_1` only if `param_2`'s validity byte is set, then always overwrites `param_1`'s validity byte to match. `std::optional`-style merge-assign helper. |
| `thunk_EXT_FUN_0000ab42` | `0xab42` | `0x6004b35e` | **`FUN_6004b35e`** -- Symmetric "prefer-valid" merge for the same tagged-union `{value,bool}` shape as `FUN_6004b344`: if `param_1` is invalid, takes `param_2`'s value (if valid); if `param_1` is valid and `param_2` is also valid, `param_1` wins (no-op on the value, early return) -- a `std::optional`-style "fill if empty" combinator. |

### Guard-and-trap (assert-then-abort) helper (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000ab36` | `0xab36` | `0x6004b352` | **`FUN_6004b352`** -- Guard-and-trap: if a validity byte at `param_1+4` is unset, calls the no-return trap `thunk_FUN_6010209a()` (marked "Subroutine does not return" by the decompiler) -- an assert-then-abort helper, same trap target as `FUN_6004b49c` and `FUN_6004bd4e` below. |

### HAL error-code translation table (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00008996` | `0x8996` | `0x600491b2` | **`FUN_600491b2`** -- **Error/status-code translation table.** A large `if`/`else` cascade mapping ~25 distinct input codes (0x385/0x386, 0x38a-0x38d, 0x1005/0x1006, 0x1b59-0x1b5b, 0x4ee8-0x4eeb, 7000, plus small values 0-6) down to a compact 0-0xf output range, with an implicit default of `2`. Called by the I2C driver (`FUN_600426c0`), the two bus-transfer helpers (`FUN_60042588`/`FUN_6004262c`), and the pin-mode-select function (`FUN_6004244c`) -- a shared HAL-error-to-application-status mapping table (0x4ee8 in particular is the exact busy/error code `FUN_600418a0` returns on failure, confirming the connection). |

### Generic block-oriented bus transaction helpers (3 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00001d6c` | `0x1d6c` | `0x60042588` | **`FUN_60042588`** -- Chunked bus-transfer loop: copies 16 words (64 bytes) at a time from a source cursor into a fixed staging buffer, dispatches via `func_0x60041854` or `func_0x6004c054` depending on a mode flag at `*param_1`, checks the result through the shared error-code translator `func_0x600491b2`, and copies a 16-byte reply back to the caller's output cursor -- generic block-oriented bus transaction helper (SPI/QSPI-flash-page or similar), 16-byte-granularity strongly suggests SPI NOR page or QSPI dummy/AHB-command payload framing rather than I2C (which is byte-at-a-time in the already-resolved thunk). |
| `thunk_EXT_FUN_0000b09a` | `0xb09a` | `0x6004b8b6` | **`FUN_6004b8b6`** -- Single 16-bit-value bus read: stages a 2-byte request through `func_0x6004c204(id, &value, 2)` and writes the result back through the output pointer on success -- small-transaction variant of the block-transfer helpers (`FUN_60042588`/`FUN_6004262c`). |
| `thunk_EXT_FUN_0000b50a` | `0xb50a` | `0x6004bd26` | **`FUN_6004bd26`** -- Fetches a `(offset,length)` pair via `func_0x6004bcf8`, then `memcpy`s (`FUN_6004bd8e`) from `offset+base` into the caller's buffer, and returns a status byte from `+0xd` -- generic "read a described sub-range" accessor, likely over the calibration-blob store noted in `bruce-io-paths.md` given the shape (offset/length descriptor + status byte). |

### Abstract stream/socket Read()-style wrapper (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00006668` | `0x6668` | `0x60046e84` | **`FUN_60046e84`** -- Generic vtable-dispatched `Read()`-style wrapper: validates the object/vtable-slot-8 pointer, and if a "connected" flag (`param_1[1]`) is set and the requested length is positive, calls the vtable read function and accumulates the byte count into a running total (`param_1[10]`); on a null object/vtable or unset connected flag it logs an error via `func_0x6004bf34` with fixed codes `0x72`/`0x73` (`'r'`/`'s'`, plausibly "read"/"start" state codes) and returns `-2`. Abstract-stream-interface shape (matches WebRTC's `rtc_base` socket/stream abstraction idiom). |

### Piecewise-linear interpolation / calibration-curve lookup (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00008ad2` | `0x8ad2` | `0x600492ee` | **`FUN_600492ee`** -- **Piecewise-linear interpolation table lookup with binary search + hysteresis-based rate tracking.** Computes a mapped Y value from an X input via `func_0x600490b4` then binary-searches (`func_0x6004c234`) a breakpoint table for the enclosing segment and linearly interpolates; separately accumulates a 1000-scaled millisecond timestamp (`FUN_60047968`-derived) into a windowed min/max rate tracker with its own binary-search-based interpolation (`func_0x6004c094`) for a second output value. Generic calibration-curve-plus-rate-limiter utility; given the binary-search-over-breakpoints shape this reads like a lookup-table-driven sensor calibration helper (cf. the calibration-blob store already documented in `bruce-io-paths.md`), though not conclusively tied to the stick/trigger ADC path. |

### Generic C++-style container/iterator-pair helper (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00008fe4` | `0x8fe4` | `0x60049800` | **`FUN_60049800`** -- Iterator-pair-to-count helper: `count = (end-begin)>>2; out = {begin, count}` (or `{0,0}` if empty) -- generic C++-style container view/span constructor, used by the audio-buffer code above and others. |

### Tick-to-time conversion / tick-delta accumulator (2 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00000eb8` | `0xeb8` | `0x600416d4` | **`FUN_600416d4`** -- Tick-to-time conversion (`clock_gettime`-shaped): branches on `param_1==1` (fill `{sec,sec_hi,nsec}` from a 64-bit `ticks*scale/divisor`, using `func_0x6004bee4` as a 64-bit divide) vs `param_1==4` (fill a plain 64-bit tick-scaled value), bracketing the tick read (`FUN_60047968`) with the spinlock pair `FUN_6004baa8`/`FUN_6004bab0` (see below). Likely the host-controller/BT-stack "get current time" primitive. |
| `thunk_EXT_FUN_00001434` | `0x1434` | `0x60041c50` | **`FUN_60041c50`** -- Tick-delta accumulator: computes elapsed ticks since last call (`*_DAT_60041c90+4 - *_DAT_60041c8c`), adds it into a running 64-bit counter (with carry), and calls `func_0x6004c414(sum_lo, sum_hi, ratio, 0)` where `ratio = *_DAT_60041c98 / _DAT_60041c9c`. Same "period-measurement" shape as the haptics-timer glue already flagged as a false lead in `bruce-io-paths.md`; likely a sibling tick-rate/period tracker rather than button/ADC-adjacent. |

### CTR-mode-shaped stream-cipher counter/XOR management (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00001e10` | `0x1e10` | `0x6004262c` | **`FUN_6004262c`** -- Streaming XOR-with-keystream loop: copies a 16-byte block from `param_5` into a fixed context buffer, then repeatedly calls `func_0x60041854` (same bus-transfer callee as `FUN_60042588` above) to refill a 16-byte keystream block and XORs it byte-by-byte into `param_2`/`param_3`, incrementing a big-endian counter/nonce (`_DAT_600426b8` downward byte-increment-with-carry loop) between blocks. Shape matches a **CTR-mode stream cipher driven by an external crypto peripheral** (the peripheral, not this code, does the block cipher; this just manages the counter/nonce and XORs the result) -- worth a closer look given `bruce-crypto.md`'s open thread about what calls the software Ed25519/EVP stack; this looks like a *different*, hardware-backed path (possibly the BT link-layer's AES-CCM, which real BT controllers implement in a crypto engine, not the BoringSSL software stack). |

### Queue-backed state-machine transition guard (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00001834` | `0x1834` | `0x60042050` | **`FUN_60042050`** -- State-transition guard for what looks like a connection/link state machine: compares a requested new state (`param_2`) against the current state at `obj+100`, using per-state expected-value checks at `obj+0x68` (byte or word depending on state 0-9 vs 7), then posts the transition through `queue__600473e4`/`queue__60047290` (send/receive with a `0x10000000` PENDSVSET kick on success) or falls back to a diagnostic log call (`func_0x6004be84`/`func_0x6004c444`/`func_0x6004bf04`/`func_0x6004c41c` -- an assert/telemetry formatter chain) if the transition is rejected. Generic state-machine-over-a-queue pattern, not yet tied to a specific link (BT HCI state machine is a plausible owner given the surrounding queue-based dispatch). |

### IRQ-guarded circular event/log-record buffer (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00001ae4` | `0x1ae4` | `0x60042300` | **`FUN_60042300`** -- Ring-buffer producer: writes a variable-length record (1-byte tag `param_1` + 4-byte value `param_2` + `param_3>>1` extra 8-byte entries pulled from the stack) into a slot-indexed buffer at `iVar8 = *_DAT_60042420 + index*8`, computing the next write index modulo a capacity (`*puVar2`) and setting an overflow flag byte on wrap. Guarded by IRQ-disable. Generic circular event/log-record buffer; only enabled while a companion flag byte is set, else logs once via `func_0x6004c304` and permanently disables itself. |

### Multi-stream buffer-level/watermark accumulator (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00002334` | `0x2334` | `0x60042b50` | **`FUN_60042b50`** -- Multi-stream buffer-level/watermark accumulator: subtracts `param_1` bytes from a running level, and if a companion "active" flag is clear, walks a fixed array of 3 stream descriptors (offsets `+4`/`0x14`/`0x24` within each, 0x10 apart) decrementing each stream's deadline by an elapsed-time field, refilling via callback `func_0x6004c21c(stream_id, size, elapsed, cb, ...)` when a deadline is hit, and tracking the minimum remaining deadline across all 3 streams. Generic triple-buffered stream scheduler; plausibly related to the audio pipeline documented below (3 buffers could be capture/AEC-reference/playback) but not conclusively tied to it. |

### memcmp (1 thunk)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_0000b554` | `0xb554` | `0x6004bd70` | **`FUN_6004bd70`** -- **`memcmp`**: byte-by-byte compare returning `0` if the full `param_3`-byte range matches, else the signed difference of the first mismatching byte pair. |

### memmove and memmove-based buffer-append wrappers (3 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00009304` | `0x9304` | `0x60049b20` | **`FUN_60049b20`** -- `memmove`-style relocate-and-append: if `[param_1,param_2)` is non-empty, `memmove`s it to `param_3` (via `FUN_6004bda4`) and returns `param_3 + length` -- a "copy a range and return the new write cursor" helper, generic buffer-builder primitive used twice in the audio path. |
| `thunk_EXT_FUN_0000952e` | `0x952e` | `0x60049d4a` | **`FUN_60049d4a`** -- Byte-identical sibling of `FUN_60049b20`: `memmove`-and-advance-cursor helper (via `FUN_6004bda4`) for a different call site in the audio path. |
| `thunk_EXT_FUN_0000b588` | `0xb588` | `0x6004bda4` | **`FUN_6004bda4`** -- **`memmove`**: forward or backward copy loop chosen based on whether source and destination ranges overlap (`param_2 < param_1 < param_2+len`) -- proper overlap-safe move, used as the underlying primitive by `FUN_60049b20`/`FUN_60049d4a`'s "copy-and-advance-cursor" wrappers and `FUN_60044440`'s DSP snapshot code. |

### Generic HAL do-atomically wrapper (2 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_00000960` | `0x960` | `0x6004117c` | **`FUN_6004117c`** -- Critical-section-guarded 2-call wrapper: saves/restores IRQ enable state around `func_0x6004111c()` + `func_0x600410b4()`. Generic HAL "do X atomically" wrapper; same two callees also appear bracketing FlexSPI LUT-programming work in `FUN_60041338`, so this is likely a small piece of that same flash-controller sequencing. |
| `thunk_EXT_FUN_00001084` | `0x1084` | `0x600418a0` | **`FUN_600418a0`** -- Peripheral register driver with a lazy one-shot init gate (`*_DAT_60041924=='\0' -> func_0x6004be5c()` init call once) then a busy-wait-poll-then-program sequence: waits for a ready bit (`*param_1 & 0x80000000`), programs 3 control fields (a `0x200`/`0x3f` mode pair and a 4-bit/6-bit sub-field pulled from a config struct), waits again, then checks a `0x200` error bit before returning a fixed error code `0x4ee8` on failure. Register-driver shape resembles the already-resolved I2C transfer thunk but targets a different control block -- possibly a second I2C/SPI-style bus; not conclusively identified. |

### Miscellaneous small getters/wrappers, not conclusively identified (10 thunks)

| Thunk | ITCM addr | Flash addr | Resolved to |
|---|---|---|---|
| `thunk_EXT_FUN_000007a6` | `0x7a6` | `0x60040fc2` | **`FUN_60040fc2`** -- USB(-like) endpoint/request dispatcher: reads a control-block's flags word, branches on several bit tests to move data between a fixed-offset buffer and a linked descriptor (`piVar6`), invoking a callback (`piVar6[4]`) via an unresolved ("too many branches") jump table. Shape (flag bits selecting IN/OUT/SETUP-style handling, per-endpoint byte buffers at `+0x40`/`+0x60`) resembles a USB device-controller ISR bottom half, not yet attributed to a specific peripheral. |
| `thunk_EXT_FUN_00001674` | `0x1674` | `0x60041e90` | **`FUN_60041e90`** -- Trivial indirect-call trampoline: `(*_DAT_6004c378)(_DAT_60041e98)` -- Ghidra could not recover the jump table at the call site ("too many branches"), so the true target is unresolved; likely a callback-table dispatch stub. |
| `thunk_EXT_FUN_00001c18` | `0x1c18` | `0x60042434` | **`FUN_60042434`** -- Small 3-way config getter: `param_1==0 -> _DAT_60042448; param_1==1 -> _DAT_60042444; else -> 0` -- selects one of two config globals (or a default) by small integer index. |
| `thunk_EXT_FUN_00001d44` | `0x1d44` | `0x60042560` | **`FUN_60042560`** -- Init helper: zero-fills (`FUN_6004bdd6`/memset) two fixed-size (0x10-byte) global buffers in sequence and returns its first argument unchanged -- generic "reset two state buffers" step. |
| `thunk_EXT_FUN_000065c4` | `0x65c4` | `0x60046de0` | **`FUN_60046de0`** -- Trivial one-word global getter, `return _DAT_60046de4;`. |
| `thunk_EXT_FUN_00008220` | `0x8220` | `0x60048a3c` | **`FUN_60048a3c`** -- Trivial pointer-indirect getter, `return *_DAT_60048a44;`. |
| `thunk_EXT_FUN_0000851a` | `0x851a` | `0x60048d36` | **`FUN_60048d36`** -- Branches on `param_2`: nonzero dispatches through an unresolved ("too many branches") jump table via `*DAT_6004c2a0`; zero instead clears bits `0x1c0000` of a status word at `param_1+8` unless a mode field (`param_1+0x90`) reads exactly `1` in its top 2 bits. Not conclusively identified. |
| `thunk_EXT_FUN_0000887a` | `0x887a` | `0x60049096` | **`FUN_60049096`** -- One-line wrapper: `FUN_6004852c(*param_1); return param_1;` -- calls the queue-drained-assert helper on a field of its argument, then returns the argument unchanged (fluent-interface-style teardown step). |
| `thunk_EXT_FUN_00008c6c` | `0x8c6c` | `0x60049488` | **`FUN_60049488`** -- One-line vtable dispatch: `if (vtable[2]) vtable[2](self, self, 3);` -- generic "finalize/close" style virtual call with a fixed mode argument. |
| `thunk_EXT_FUN_0000b5ca` | `0xb5ca` | `0x6004bde6` | **`FUN_6004bde6`** -- Small bitfield test: fetches a base via `thunk_FUN_600cdbbc`, and if bits `0-1` of a byte at `[base+param_1+1]` equal `1`, adds `0x20` to `param_1` -- looks like a small state-dependent offset adjustment (e.g. selecting between two 0x20-byte-separated sub-records based on a mode field); not conclusively tied to a specific subsystem. |

## Session 17: `tasks.c` — the rest of the FreeRTOS task-lifecycle API (now 19/19, file complete)

`bruce-decompile-status.md`'s §3a cheap-win table flagged `tasks.c` (1,736 remaining bytes, 4/19 already done) as a priority — it had been omitted from the prior top-10 despite outranking the listed #10. All 15 remaining functions decompiled this session (`analysis/decomp/tasks__*.c`); the file is now fully decompiled.

This is **not** new territory the way the sections above are — it's the natural extension of the same ITCM-resident FreeRTOS core this doc already maps (`tasks__60047aa8`, `tasks__60047b98`, `tasks__60047d80`, `tasks__600ca1f8` were already resolved as thunk targets in sessions 5–6, table above). The 15 new functions are the *rest* of `tasks.c`'s public API: task creation, deletion, suspend, priority-set, and the notification API — not more hot-path primitives. Every one of these calls back into HAL/list primitives already identified in the thunk tables above (critical-section enter/exit at `0x60048580`/`0x600485c8`, list insert/remove at `0x6004bb1a`/`0x6004bb60`, `vPortFree` at `heap_5_improved__600488f4`, `vTaskSwitchContext` at `0x60047be8`, the `std::optional`-shaped `{value,bool}` merge helpers at `0x6004b344`/`0x6004b352`/`0x6004b35e`), which is strong independent corroboration this is genuinely FreeRTOS `tasks.c` and not a look-alike.

| Function | Size | Identification | Confidence |
|---|---:|---|---|
| `tasks__60047974` | 256B | **`xTaskIncrementTick()`** — if the scheduler isn't suspended, advances the tick count (with overflow → swaps the delayed/overflow-delayed list pair), walks the delayed-task list waking any task whose wake time has passed, and returns whether a context switch is needed; if suspended, just counts a pending tick (`uxPendedTicks++`). Called from the tick-hook wrapper `FUN_600485f8` (session 5/6) and from `tasks__60047aa8`'s "catch up on pended ticks" loop below. | high |
| `tasks__60047aa8` | 194B | **`xTaskResumeAll()`**, not merely a generic "yield" as the session-5 write-up above guessed before the rest of the file was legible — decrements the scheduler-suspend nesting count (incremented by `FUN_60047958`, i.e. `vTaskSuspendAll()`), and on the last nesting level: moves every task off the pending-ready list back onto its real ready list, replays any pended ticks by calling `tasks__60047974` in a loop, and requests a context switch (PENDSVSET) if either produced one. Still correctly described as "the universal yield/re-schedule call" in effect (it's what every blocking primitive calls at the tail of its critical section), just with the precise FreeRTOS name now attached. | high |
| `tasks__60047cac` | 44B | **`vTaskPlaceOnEventList()`** — sorted insert (`FUN_6004bb32`, priority-ordered) of the calling task onto an event/wait list, then delegates to the delayed-list insert helper. Distinguished from the next row by using the *sorted* list-insert. | high |
| `tasks__60047d3c` | 54B | **`vTaskPlaceOnEventListRestricted()`** — same shape as `60047cac` but uses the *unsorted* tail-insert (`FUN_6004bb1a`, `vListInsertEnd`-shaped) and takes an explicit "wait indefinitely" flag (`param_3`) that forces the tick count to `0xffffffff` — matches the real function's restricted/unsorted variant used by stream buffers and other single-waiter primitives. | high |
| `tasks__60047ce4` | 72B | **`vTaskPlaceOnUnorderedEventList()`** — sets the top bit (`\| 0x80000000`) on the item value before inserting, exactly FreeRTOS's `taskEVENT_LIST_ITEM_VALUE_IN_USE` marker distinguishing event-group "AND"/"OR" wait semantics from a plain wake-time value. Only real caller is the event-group wait/clear helper `FUN_6004703c` already identified in the table above. | high |
| `tasks__60047ea8` | 98B | **`xTaskRemoveFromUnorderedEventList()`**-shaped (a queue/stream-buffer receive path's "is data available yet, else adjust remaining wait time" check) — brackets a critical section, and either signals immediately (`0xffffffff` sentinel) or recomputes remaining ticks-to-wait against an elapsed-time global, waking the task early via `FUN_60047e90` if time's already up. | medium |
| `tasks__60048014` | 138B | **`prvAddCurrentTaskToDelayedList`-adjacent list-requeue helper**: decrements a caller's "critical nesting"-shaped counter, and on the last release, if the task's list-item value changed, removes it from its priority ready list (clearing the ready-priority bitmap bit if now empty) and reinserts it into the list matching its (possibly new) item value. | medium |
| `tasks__600ca1b4` | 58B | **`prvDeleteTCB()`** — switches on the TCB's allocation-type byte (`+0xc9`): `0` = fully dynamic (frees both the stack buffer at `+0x30` and the TCB itself, both via the confirmed `vPortFree`, `heap_5_improved__600488f4`), `1` = static stack + dynamic TCB (frees only the TCB), `2` = fully static (no-op, matches `tasks__600ca1f8`'s creation path which sets this byte to `2`). | high — corroborated directly by the already-identified `vPortFree` callee |
| `tasks__600ca27c` | 164B | **`vTaskDelete()`** — removes the task from its ready/state list (clearing the ready-priority bitmap bit if it was the last member), removes it from any event list it was blocked on, increments a deletion/task-count bookkeeping value, and either (a) if deleting itself: moves it onto the "waiting termination" list for the idle task to reap and increments a counter, or (b) if deleting another task: decrements the live task count and calls `tasks__600ca1b4` (`prvDeleteTCB`) directly. If the task being deleted is the one currently running, requests a context switch via the PENDSVSET MMIO write (can't safely call `vTaskSwitchContext` synchronously while still executing on the doomed stack). | high |
| `tasks__600ca350` | 98B | **`eTaskGetState()`** — compares the task (or current task if `NULL`) against `pxCurrentTCB` (→ `eRunning`=0), then its state-list-item container against the known delayed/overflow-delayed lists (→ `eBlocked`=2), the suspended list (→ `eSuspended`=3, or `eBlocked`=2 if it still has an active event-list wait), the terminated-tasks list or a null list pointer (→ `eDeleted`=4), else defaults to `eReady`=1 — the exact enum ordering (`eRunning, eReady, eBlocked, eSuspended, eDeleted` = 0..4) real FreeRTOS uses. | high |
| `tasks__600ca4ac` | 136B | **`xTimerCreateTimerTask()`-shaped**: creates a task via the generic create-core (`tasks__600ca1f8`, see below) and, if `timers__600cacb8()` (presumably the timer command-queue creation) reports success, does a raw (non-nesting-counter) IRQ-disable/`BASEPRI`-raise/barrier/IRQ-enable block to atomically install the timer-daemon's globals (deadline, sentinel, ready flags) before calling two more not-yet-decompiled helpers (`FUN_600582f8`, `FUN_600cc5ac`). Only caller is `init__600cbdd4`. Moderate confidence on the exact FreeRTOS name (the create-then-configure order doesn't exactly match the textbook `timers.c` implementation, and this function is itself attributed to `tasks.c` rather than `timers.c` — plausibly a compiler-inlined/merged variant, or file misattribution isn't ruled out) but very high confidence it's "create and initialize the RTOS timer-service daemon task". | medium |
| `tasks__600ca57c` | 30B | Trivial accessor: returns `(task ?: pxCurrentTCB) + 0x34` — a pointer into the TCB at a not-yet-identified embedded-struct offset. Single caller (`FUN_60083730`, not yet decompiled). Purpose of TCB+0x34 unresolved. | low |
| `tasks__600ca5a8` | 24B | **`xTaskGetCurrentTaskHandle()`** — asserts and returns `pxCurrentTCB` directly, no computation. | high |
| `tasks__600ca5cc` | 176B | **`vTaskSuspend()`** — removes the task from its ready list (clearing the ready-priority bitmap bit if empty) and any event list, inserts it onto the suspended-task list, clears a per-task notification-state byte if it was mid-wait, and if suspending the *currently running* task: calls the scheduler core `vTaskSwitchContext` (`0x60047be8`, resolved above) **directly and synchronously** when the scheduler isn't suspended and another ready task exists — as opposed to `vTaskDelete`'s indirect PENDSVSET request, this is the concrete FreeRTOS-implementation-detail difference between "yield now, I'm about to be freed anyway" (delete) and "yield now, I can call the scheduler directly because I'm still a valid stack to switch away from" (suspend) — a nice confirmation that `600ca27c`/`600ca5cc` really are the delete/suspend sibling pair the shapes suggested. | high |
| `tasks__600ca9e8` | 156B | **`vTaskPrioritySet()`** — clamps the requested priority to not go below the task's base (mutex-inheritance) priority, and if the effective priority actually changes and the task isn't currently priority-inherited: updates the priority field, and if the task is presently on its (old-priority) ready list, moves it to the new one. | high |
| `tasks__600cab44` | 232B | **`xTaskGenericNotify()`** — the "give" half of the Task Notification API: raw critical section (disable IRQ, raise `BASEPRI`, barriers), returns the previous notification value through an out-param, switches on the action code (1=`eSetBits`/OR, 2=`eIncrement`, 3=`eSetValueWithOverwrite`, 4=`eSetValueWithoutOverwrite` — refuses if already-notified), sets notify-state to "notified", and if the task was previously blocked waiting on the notification, removes it from its wait list and flags a higher-priority-task-woken condition through either an out-param or a global. Action-code numbering and "refuse to overwrite if already notified" logic is an exact match for the real FreeRTOS enum. | high |
| `tasks__600ca1f8` (previously resolved, session 5/6) | 124B | Re-confirmed as the **generic task-create core** (`xTaskCreate`/`xTaskCreateStatic`-shared internals): validates the stack-size/TCB-buffer params, stores the stack size at TCB+0x30 and the allocation-type byte at TCB+0xc9 (the same byte `600ca1b4`'s `prvDeleteTCB` switches on — direct confirmation these two functions are a matched create/delete pair), then calls two not-yet-decompiled helpers `FUN_600ca0fc` (`prvInitialiseNewTask`-shaped) and `FUN_600c9fd8` (`prvAddNewTaskToReadyList`-shaped). | high |

**Net effect:** `tasks.c` is now a complete, from-source-shape-confirmed map of FreeRTOS's task lifecycle (create/delete/suspend), scheduling (`xTaskIncrementTick`/`xTaskResumeAll`/`eTaskGetState`/`vTaskPrioritySet`), the three event-list placement variants, and the full Task Notification API (`xTaskGenericNotify`) — layered directly on top of the list/critical-section/`vPortFree` HAL primitives this doc already resolved. Two loose ends: `tasks__600ca57c`'s TCB+0x34 accessor (single caller, not yet chased) and the exact identity of `tasks__600ca4ac`'s two undecompiled callees (`FUN_600582f8`, `FUN_600cc5ac`) if anyone wants to fully nail down the timer-daemon-creation path.

## Session 21: `receiver.cc` — the SPI mic-receive task, now decompiled (4/4)

`receiver.cc` was `bruce-decompile-status.md`'s §3a rank-2 cheap-win target (812 remaining bytes, 0/4). All 4 attributed functions are now decompiled (`analysis/decomp/receiver__*.c`), and they turn out to be exactly the piece this doc's "Embedded microphone/audio DSP pipeline" section above was missing: the **object lifecycle and ring-buffer plumbing around `FUN_60046ccc`** (the mode-dispatch entry point already identified above via its `"audio"`/`"ReceiverBuffer"`/`"Pausing microphone receiving due ..."` literal strings).

- **`receiver__6007f540`** (438B) — **`GetOrCreateReceiverBuffer()`, a lazy singleton constructor.** Locks a module-global guard (`thunk_EXT_FUN_0000b4c2`/`00007d10`, the same mutex-bracket pair used throughout the codebase), and if no instance exists yet, allocates a **`0x9dc` (2,524)-byte** object, wires up a FreeRTOS queue (`queue__600c9de4(5, 1, ...)` — depth-5, matching `bruce-io-paths.md`'s already-resolved `queue.c` primitives), sets a working buffer pointer/size (`+0x62`→`+100`, size `0x200`), and — critically — sets a ring-buffer bound pair at offsets **`+0x26d`/`+0x26e`** to `base` and `base+0x780`. **`0x780` = 1920 bytes is the exact PCM-chunk size `FUN_60046ccc` (this doc, above) forwards to `func_0x600428b8` in its mode-0 path** — direct, byte-exact confirmation that this is the constructor for the `ReceiverBuffer` object `FUN_60046ccc` operates on. After building the object it inserts it into a module-global intrusive singly-linked list (a manual head-insert using the same `thunk_EXT_FUN_00001680` refcount/release primitive seen elsewhere) and starts the actual FreeRTOS task via **`FUN_6010177a(obj, DAT_6007f720, 0x1b)`** — the same task-creation helper `synapse_audio_processor.cc` (`bruce-audio-subsystem.md`) uses for `"SynapseAEC_Task"`, here with priority `0x1b` (27) and (per the string confirmed in the next function) the name **`"SpiReceiverTask"`**. On creation failure, logs and tears the object back down via `FUN_600decf8`. Confirms the mic-capture pipeline is fed over **SPI**, not I2S/PDM directly off the codec — a new, concrete detail this doc's DSP-pipeline section didn't previously establish.
- **`receiver__6007f454`** (162B) — **Shutdown/join.** Signals the running task to stop (`thunk_EXT_FUN_00006a74(*piVar1+0xb0, ..., 0xffffffff, 0)`), then waits up to 500 ticks for it to actually exit (`FUN_601017b4`); on timeout, formats and logs the literal string **`"Timed out joining SpiReceiverTask"`** (`PTR_s_Timed_out_joining_SpiReceiverTas_6007f50c`) — this is the exact string that names the task and confirms `receiver__6007f540`'s `DAT_6007f720` task-name argument. Either way (timeout or clean exit) tears the singleton down via `FUN_600decf8`.
- **`receiver__6007ec48`** (134B) — **Fault/status-event reporting front-end**, gated by a flag-word check (`*(DAT_6007ec5c+4) & 0x1ff`). If a fault is already latched, routes either through a synchronous log-flush (`FUN_600d53ae`) or an async post (`FUN_6007efc0`) depending on whether a receiver instance currently exists; otherwise (the normal-operation path) forwards `(param_1, param_2, param_3)` into `FUN_6007f510` (an accessor for the current receiver instance) and, if found, calls `FUN_600debb4` with a packed status-tag byte (`2`) — consistent with a **SPI-transfer-completion callback** reporting per-transfer status up into the receiver object, called from either ISR or task context (hence the fault-flag gate for "don't touch shared state from the wrong context").
- **`receiver__6007f1a4`** (78B) — **The ring-buffer commit step, almost certainly called from the SPI/DMA ISR itself.** Compares the write-cursor field at object offset **`+0x9c0`** against the consumed-cursor field at **`+0x9c8`**; if they're equal (ring buffer full — no room for the next chunk), logs an assert-style error (`FUN_6010165c`) instead of committing. Otherwise, brackets a **double `DataMemoryBarrier`** (the ISR-safe memory-ordering idiom used elsewhere in this doc's exclusive-monitor primitives) around a call to `FUN_600debc8(obj+0x9bc, field@+0x990, field@+0x994, obj+0x998, &local_flag, param_2, param_3, packed_flag)` — advancing the buffer and dispatching the newly-received chunk. **Offsets `0x9c0`/`0x9c8`/`0x9bc`/`0x9d0` all fall within the same `0x9dc`-byte object `receiver__6007f540` allocates** (near its very end, `0x9dc` = 2,524 bytes total) — i.e. this is the read/write cursor pair guarding the tail of that exact object, not a separate structure.

**Net:** `receiver.cc`'s four functions are the **construct → run → receive-commit → join/destroy** lifecycle of a dedicated `"SpiReceiverTask"` FreeRTOS task that owns a 1920-byte-windowed ring buffer and feeds `FUN_60046ccc` (this doc's already-identified mic-DSP entry point) — closing the loop this doc's "Embedded microphone/audio DSP pipeline" section left open (which class of peripheral feeds the pipeline). Confirms the pipeline's front door is **SPI**, adds a concrete task name/priority, and gives byte-exact corroboration (the `0x780`/1920-byte bound) tying the object layout directly to the already-decompiled DSP entry point. Still open: the actual SPI peripheral driver / DMA setup underneath this task (not part of `receiver.cc` itself — `FUN_6007f510`/`FUN_6007efc0`/`FUN_600debb4`/`FUN_600debc8` are all still undecompiled and would be the natural next hop), and no caller into `receiver__6007f540` was found this session (same "present but caller not yet traced" pattern as several other subsystems in this project).

## Open

- **All 142 distinct `thunk_EXT_FUN_0000xxxx` names in `bruce_functions.csv` are now resolved** (10 in session 5, 132 in session 6 — see table above). The doc's earlier "155 veneers" figure counts individual veneer *stub instances* at the flash veneer addresses (`0x6013cee8`–`0x6013d4e0` etc.), not distinct ITCM targets; some stubs duplicate a target another stub already reaches, which is why 142 unique targets fully account for all 155 stub instances (no unresolved gap remains — the ~13 "extra" stubs are just duplicate veneers into already-resolved targets, unconfirmed individually but structurally accounted for).
- **One function, `FUN_60042c9c` / `xEventGroupSetBits`, has a genuinely discontiguous/split body** — a veneer (`thunk_EXT_FUN_0000691c`) targets a real code fragment physically ~0x4500 bytes away from the function's nominal entry point (confirmed by hand-disassembling the raw bytes at `0x60047138`, which begin with a real `push.w {r4-r8,lr}` prologue and contain substantive loop logic, not a stub), and Ghidra's `getFunctionContaining()` correctly attributes both fragments to the same `Function` object. **Correction (QA pass, same session):** the original write-up also described both `heap_5_improved__*` allocator functions as having split bodies via the same mechanism — that was wrong. Their extra thunk targets (`0x6004904e`, `0x60049060`, `0x6004bd46`, `0x6004bd4a`) are, per manual Thumb-2 disassembly, nothing but chained bare 4-byte `B.W` branch instructions (ordinary in-blob thunks, one to three hops deep) that jump straight to the real, single-region function entry — not fragments of the body itself. Both `.c` files on disk are complete, non-truncated, single-region functions. The batch-decompile output-file-naming collision that prompted re-decompiling them at their canonical entry addresses did happen and was a real (and correctly applied) fix, but its cause was Ghidra resolving multiple bare-branch-stub addresses to/through the same ultimate target, not a physically split function body. See the corrected methodology notes under both row groups above for the byte-level evidence. Worth double-checking with `getFunctionContaining()`/thunk-chain-following in Ghidra directly (not done here, since this QA pass avoided running `analyzeHeadless`) next time the project is opened, to confirm Ghidra itself also resolves these as thunk chains rather than a single merged Function object.
- The 236-byte gap between the copy's flash end (`0x6004c614`) and the next function boundary (`0x6004c620`) is presumably alignment padding / a data literal, not investigated further.
- The `.data`/`.bss`/stack-paint loops in `Reset_Handler` were read off the literal pool for cross-validation but not written up in depth — out of scope for this thread (ITCM only).
- **New subsystem surfaced by this sweep, not yet cross-referenced against `bruce-io-paths.md`:** a coherent on-device microphone/audio DSP pipeline (FFT, IIR/CIC filters, Wiener-style noise-suppression gain control, `receiver.cc`'s `"Pausing microphone receiving due ..."` log path) — see the "Embedded microphone/audio DSP pipeline" group above. This is a new finding, not one of the six open threads listed in CLAUDE.md; flagged here for whoever picks up thread priority next.
- Also surfaced: a probable **runtime FlexSPI/QSPI flash driver** (LUT programming + cache-maintenance + chunked write, distinct from the boot-time FCB), **IOMUXC_GPR (0x400AC000)** and **CCM (0x400FC000)** bitfield read/write helpers, and the **FreeRTOS PendSV handler + scheduler core** (`FUN_6004166c`/`FUN_60047be8`) — none of these were previously mapped. No direct ADC or GPIO MMIO register access was found in this batch (the two peripheral bases confirmed are IOMUXC_GPR and CCM, not ADC or plain GPIO), and the closest thing to NVIC interaction is `FUN_60048628`, a `configASSERT`-only priority *check*, not a live handler — so open thread #1 (runtime button/ADC read path) is still unresolved, though the ISR-context critical-section functions found (`FUN_6004901e`, the NVIC-priority assert) confirm *some* ISR-driven code exists in this blob worth following up on.
