# CLAUDE.md — Stadia Controller firmware RE

Orientation for working in this repo. Deep technical findings live in `analysis/*.md`; read those for detail rather than duplicating them here. If you're picking this project up fresh (e.g. a different model taking over for a stretch), also read `HANDOFF.md` — it explains the multi-session orchestration workflow that's been producing progress here, not just the technical state.

## What this is
This started as a dump of the Google Stadia Controller's official firmware (`bruce_*` = Bluetooth-mode, plus flashloaders). The active work is **reverse-engineering and documenting that firmware** — a preservation effort the owner cares about in its own right. The original "make it work on a PS5 (spoofing a DualShock 4)" idea is now a secondary/aspirational angle: it's blocked at the **PlayStation console-authentication layer**, not by anything in the Stadia firmware (see `analysis/` + the detailed reasoning in Claude memory). Don't relitigate the PS5 feasibility question without new information.

Scope: this is legitimate RE of firmware the owner possesses, for understanding/preservation/personal-use compatibility. Keep it that way.

Work happens on the **`firmware-re`** branch (not `main`).

## Hardware in one line
NXP **i.MX RT106x** (Cortex-M7), execute-in-place from a 16 MB Winbond **W25Q128JW** QSPI flash mapped at **0x60000000**; Broadcom **BCM43458** BT/Wi-Fi radio; FreeRTOS; firmware is HAB-signed (RSA-4096) but **not encrypted**. Full detail in Claude memory (`stadia-hardware-baseline`) and `analysis/firmware-map.md`.

## Repo layout
- `bruce_pvt_a_prod_signed.bin`, `restricted_ivt_flashloader.bin`, `flashloader_fcb_get_vendor_id.bin` — original committed dumps (repo root).
- `reference_firmware/` — firmware recovered from the Wayback Machine (Google's asset host is dead): `bruce` dev/stage/prod (code-identical, differ only in the HAB signature block), the two `gotham` Wi-Fi OTA images (`pvt-a`/`pvt-b`), and the W25Q128JW FlexSPI config block.
- `analysis/` — all RE work:
  - `firmware-map.md` — top-level structural map; `bruce` (BT) vs `gotham` (Wi-Fi, a full embedded WebRTC cloud-streaming client).
  - `bruce-ghidra.md` — Ghidra workspace details, function/module map, re-run recipes.
  - `bruce-io-paths.md` — decompiled HID input/output + rumble + input-subsystem findings; **the current working document**.
  - `bruce-crypto.md` — the embedded BoringSSL crypto stack (SHA-512, Curve25519/Ed25519 field+point arithmetic, the generic `EVP_PKEY_ASN1_METHOD` registry) — traced bottom-up from the largest functions in the image.
  - `bruce-itcm.md` — the boot-time ITCM copy (`Reset_Handler`'s `memcpy(0x0, 0x6004081c, 0xbdf8)`) and resolved `thunk_EXT_FUN_0000xxxx` HAL primitives.
  - `bruce-decompile-status.md` — **the master tracking doc for the full-decompile goal**: data-driven attributed/decompiled coverage stats (by function count and by byte size) across all ~5,000 functions, per-source-file gap table, and ranked list of the biggest unattributed contiguous address ranges. Regenerate/re-consult this before picking the next chunk of work rather than guessing.
  - `bruce-misc-functions.md` — grab-bag write-ups for decompiled functions that don't fit an existing doc's theme yet (haptics tick-path detail, sleep driver, misc input-ctor helpers, etc.).
  - `bruce-log-buffer.md` — the codebase-wide diagnostic log/message-`Buffer` framework (session 14): a small message-buffer class + rate-limited ring-buffer emission engine called directly from 64 of 139 attributed source files — very likely the mechanism generating the `__FILE__` string leaks this whole project's attribution technique depends on. Also notes two smaller extensions of existing subsystems found the same session: more BoringSSL runtime support (`ERR_put_error`/`OPENSSL_malloc`-shaped code, extends `bruce-crypto.md`) and the GKI buffer-pool allocator underlying BTA/BTE (extends `bruce-bta-stack.md`).
  - `bruce-state-machine.md` — `state_machine.cc` (session 17): a generic C++ hierarchical-state-machine (statechart) base class — run-to-completion event dispatch, least-common-ancestor transition algorithm, on-enter/on-exit virtual dispatch — plus its concrete instantiation driving the BLE adapter's pairing/advertising-mode lifecycle (ties `adapter.cc`, `advertiser.cc`, and `gatt_manager_task.cc` together).
  - `bruce-audio-subsystem.md` — (session 18) a full USB-host-mode USB Audio Class (UAC1/UAC2) driver (`usb_host_audio.cc`, `usb_host_audio_topology.cc`, `usb_audio_receive.cc`) plus a voice-path policy layer (`audio_states.cc`) that selects a "Synapse" AEC/AGC/NS backend and wires USB mic/speaker endpoints into it — almost certainly shared-platform code, not confirmed-active on the physical (BT-only, no USB-host-port) controller hardware.
  - `ghidra_scripts/` — headless GhidraScripts (Java) + `decode_hid_descriptor.py`.
  - `ghidra/` — the Ghidra project **and generated CSV/TXT outputs**. The project database (`*.gpr`, `*.rep/`) and `*.log` are **git-ignored** (regenerable); the `bruce_*.csv`/`.txt` outputs are committed.
  - `decomp/` — decompiled C for functions of interest (committed).

## Ghidra workspace — how to drive it
Ghidra 12.x (Homebrew). **Must run on JDK 21** (the system default JDK is too new and Ghidra will misbehave):
```bash
export JAVA_HOME="/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home"
HL=/opt/homebrew/opt/ghidra/libexec/support/analyzeHeadless
```
Load params for any Stadia image: raw `BinaryLoader`, **base 0x60040000**, processor **`ARM:LE:32:Cortex`** (Cortex-M7 Thumb-2). (`gotham` pvt-b links at 0x60840000 instead.)

Import + analyze + map (from repo root):
```bash
"$HL" analysis/ghidra bruce -import reference_firmware/bruce_pvt_a_prod_signed.bin -overwrite \
  -loader BinaryLoader -loader-baseAddr 0x60040000 -processor ARM:LE:32:Cortex \
  -scriptPath analysis/ghidra_scripts -preScript SeedCortexM.java -postScript DumpMap.java
```
Then run scripts against the existing project without re-analyzing (`-process ... -noanalysis`):
```bash
# who references a data address (e.g. a string)
"$HL" analysis/ghidra bruce -process bruce_pvt_a_prod_signed.bin -noanalysis \
  -scriptPath analysis/ghidra_scripts -postScript FindRef.java 0x601230e7
# decompile functions -> analysis/decomp/*.c (creates a function if none exists at the addr)
"$HL" analysis/ghidra bruce -process bruce_pvt_a_prod_signed.bin -noanalysis \
  -scriptPath analysis/ghidra_scripts -postScript Decompile.java 0x60073bf0 0x600656a0
```
Open the GUI with `JAVA_HOME=… /opt/homebrew/opt/ghidra/libexec/ghidraRun` and open the `analysis/ghidra` project. The scripts hardcode output paths under `/Users/kyle/Repos/stadiaRawBtFw/analysis/`; adjust if the repo moves.

Custom scripts: `SeedCortexM` (parse the Cortex-M vector table before analysis), `DumpMap` (function/string CSVs), `AnnotateFromFilenames` (attribute functions to source files via leaked `__FILE__` strings — the core navigability trick), `FindRef` (xrefs to an address), `Decompile` (dump C + callers/callees), `MapItcmAddr` (map an ITCM address 0x0000xxxx to its flash address per `bruce-itcm.md` — accepts bare offsets or `thunk_EXT_FUN_0000xxxx` names), `PrintData` (session 15: reads/resolves a data pointer at a given address — useful when Ghidra hasn't auto-resolved a string/table xref).

**Concurrency gotcha (learned the hard way, session 14):** never launch two agents that both plan to run `analyzeHeadless -process` in the same wave, even with "check for a lock/wait if busy" instructions — that heuristic doesn't reliably converge (an agent can loop indefinitely re-checking rather than proceeding). Serialize Ghidra-touching work explicitly: one Ghidra agent per wave, full stop; pair it with genuinely non-Ghidra work instead.

## Conventions & gotchas
- **Never commit the Ghidra DB** (it's ~23 MB and regenerable) — the `analysis/.gitignore` handles `*.gpr`, `*.rep/`, `*.log`. Do commit the scripts, `.md` writeups, CSV/TXT outputs, and `decomp/*.c`.
- Commit messages end with the `Co-Authored-By: Claude Opus 4.8` trailer (see git log for the pattern). Work on `firmware-re`; don't commit to `main`.
- **`getImageBase()` is 0** for a raw `BinaryLoader` import — use `getMinAddress()` for the load address (this bit `SeedCortexM` once).
- Function attribution comes from leaked `__FILE__` strings, so a function's *file* is reliable but the file's *purpose* may not be — verify by reading. Known traps: **`keys.cc` is a typed config key-value store, not gamepad buttons.** **`bcm.c` is BoringSSL's `crypto/fipsmodule/bcm.c` ("BoringCrypto Module" FIPS aggregate file) — BIGNUM + generic `EC_GROUP`/`EC_POINT`/`EC_KEY` code — not a Broadcom BT chip HCI/patchram driver, despite the name** (session 9, `analysis/bruce-crypto.md`; the real Broadcom HCI transport layer is in `analysis/bruce-bta-stack.md`).
- Pervasive `thunk_EXT_FUN_0000xxxx` calls target low addresses (0x0000xxxx) — these are hot HAL/RTOS routines (I2C, queues, memcpy/memset, FreeRTOS task-yield) copied into ITCM at boot. **Resolved (session 5, `analysis/bruce-itcm.md`):** `Reset_Handler` copies flash `0x6004081c`–`0x6004c614` to ITCM `0x0`–`0xbdf8`, so `flash_addr = itcm_addr + 0x6004081c` — the bytes are already in the static image at that flash offset, no overlay needed. Use `MapItcmAddr.java` then `Decompile.java` on the resulting flash address to resolve any given thunk; 142 of 155 done so far (all distinct thunk targets — the remaining gap is duplicate veneer stubs into already-resolved targets, not unresolved code).
- The HID report descriptor is embedded at flash **0x60103BA0**; `analysis/ghidra_scripts/decode_hid_descriptor.py` decodes it.

## Current status & next targets
**Standing goal (session 6+): fully decompile and document `bruce` to the extent it could be recompiled bit-for-bit given Google's signing keys.** Per `analysis/bruce-decompile-status.md` (regenerated session 13): of ~4,995 functions, **41.4% of the image's code by byte size is now understood in some way** (up from 22.0% at session 6), climbing further still with sessions 13-15's work (BTA sweep, `tasn_dec.c`/`adapter.cc`/`keys.cc` fully decompiled, log-buffer framework). A more precise "truly unidentified, no module guess at all" figure was **47.3%** as of session 13. That doc is the live tracking checklist but is now mildly stale again (sessions 14-15 added more) — consult it, but expect the real number to be a bit higher; regenerate when convenient rather than trusting the exact figure. Treat this as a large, ongoing, multi-session effort — delegate well-scoped chunks to subagents rather than doing bulk decompilation inline, and only one agent at a time may hold the Ghidra project open (`analysis/ghidra/bruce.rep` is a single-writer file-based DB — concurrent `analyzeHeadless -process` invocations will conflict).

The BT firmware's gamepad I/O is now almost entirely mapped: HID report format (input ID 3 / rumble output ID 5), calibration, rumble/haptics, and — as of sessions 8-12 — the full button/ADC read path (`analysis/bruce-io-paths.md`). Sessions 4-7 mapped three more major subsystems: the BoringSSL crypto stack (`analysis/bruce-crypto.md`), the ITCM boot-copy blob (fully resolved, `analysis/bruce-itcm.md`), and — the big one — the statically-linked **Broadcom BTA/BTE Bluetooth stack**, located and identified with very high confidence at `0x600921b8`–`0x600c9cc4` (HCI/BTM/GATT/SDP/SMP/L2CAP/EC-crypto; 327 of 1,076 functions decompiled, ~46% by byte size — `analysis/bruce-bta-stack.md`). Open threads, in rough priority:
1. **Report-packing function — 95% closed; the last hop is now genuinely exhausted for headless work (confirmed session 16).** Five sessions (8-12) fully traced the input path: `InputTask` (RAM `0x200108a8`, `xTaskCreateStatic`'d from `main__60051240`) polls 17 GPIO pins on a `HidPollMs` timer (`FUN_600432e8` → `FUN_60043028` → `FUN_60042ee0` → `FUN_600494e6`, confirmed debounced + active-low-inverted — buttons are **polled, not IRQ-driven**, despite a real IRQ-callback mechanism sessions 8-9 mapped that turned out to be unused/no-op'd for this purpose), samples calibrated ADC axes + 2 trigger-threshold booleans in the same function, and bit-packs everything via `FUN_60042d44` into a 16-slot ring buffer. **The one remaining hop** — what actually reads that ring buffer and calls the confirmed transmit function `hid_input_target__60058aa8` — traces to a GATT characteristic-callback slot (`thunk_FUN_600df4b0`, installed by `FUN_600813cc` on a GATT-ish object from `gatt_server__60081ab4`) whose runtime invoker resisted three sessions of headless static-analysis techniques (literal scan, function-boundary correction, generic-engine tracing) — consistent with real virtual dispatch through an untyped C++ object. **Session 15's CCCD lead (`adapter.cc`'s subscribe/unsubscribe virtual dispatch) was chased down in session 16 and closed as a dead end**: found its caller (`FUN_60069dd4`, a real BTA_GATTS-event dispatcher, itself uncalled by anything static analysis can find) and its vtable-dispatch target (the `adapter.cc` singleton's own BD_ADDR/readiness field, RAM `0x20007bf8+0x110` — confirmed, byte-level, to be a *third* object distinct from both `0x2000385c` and `gatt_server__60081ab4`'s `0x20003a60`) — it does not connect to the transmit chain. Four independent leads across four sessions (10, 11, 12, 16) have now each hit a confirmed dead end or an invisible-indirect-call wall. **This needs interactive Ghidra GUI work with manual struct-typing to close — headless sessions on this specific sub-thread are no longer a good use of time.** Confirmed useful negative: no HOGP/HID-over-GATT strings exist anywhere in the image. Full chain + all evidence in `analysis/bruce-io-paths.md` sessions 8-12 and 16, and `analysis/bruce-bta-stack.md`'s `adapter.cc` section (session 15).
2. ~~Find the real main event/dispatch loop~~ — **resolved for the BT stack (session 7):** `FUN_600a9fce` is `btu_task`, BTE's GKI-mailbox message pump (self-logs `"btu_task"`; dispatches HCI events and ACL/L2CAP data). The overall RTOS scheduler-level main loop (if distinct from this) is still unconfirmed, but the BT-stack dispatch loop specifically is done — the earlier `FUN_6004cdb8` "biggest unread function" lead was a dead end (SHA-512 compression, see `analysis/bruce-crypto.md`).
3. ~~Map the ITCM blob~~ — done (sessions 5–6): boot copy is `memcpy(0x0, 0x6004081c, 0xbdf8)`; all 142 distinct veneer targets resolved (`analysis/bruce-itcm.md`). Surfaced a previously-unknown on-device microphone/audio DSP subsystem (FFT, IIR/CIC filters, WebRTC-style noise-suppression gain control — shared with `gotham`, likely for the Assistant-button mic) plus the runtime FlexSPI/QSPI flash driver and the FreeRTOS PendSV/scheduler core.
4. ~~Find what calls into the EC crypto stack~~ — **resolved (session 7):** traced `smp_data_ind` → `smp_sm_event` (SMP's FSM dispatcher) → BLE-RAND-seeded ECDH keypair generation → confirmed EC scalar multiplication → the P-256/P-192 modular-reduction helpers, end-to-end. SMP's LE Secure Connections pairing is the real runtime consumer of the P-256 stack found in item 5 below — a more likely "live" curve than the Ed25519/X25519 stack in `analysis/bruce-crypto.md`, which still has no confirmed caller.
5. **Continue the BTA/BTE stack sweep** (`analysis/bruce-bta-stack.md`) — 418+ of 1,076 functions decompiled as of session 13 (~64% by byte size), climbing further with session 15's `adapter.cc` work. The leaked-string-table identification lead is now exhausted; remaining functions need shape/call-graph inference (lower confidence per function — QA has been holding up well on this so far, keep pairing new sweep waves with QA passes). L2CAP is located (`0x600b5xxx`–`0x600b9fxx`) and its channel-state-machine gaps filled (session 13). BTA_GATTS server API surface substantially covered.
6. ~~`bcm.c` cheap win~~ — **done (session 9):** all 52 functions decompiled and read. Turned out to be a misattribution, not a Broadcom BT driver — it's BoringSSL's BIGNUM + generic EC (`EC_GROUP`/`EC_POINT`/`EC_KEY`) library code (`analysis/bruce-crypto.md`'s session-9 section has the full function map and evidence).
7. `FUN_601054dc` (`analysis/bruce-ghidra.md`) is confirmed (session 6) to be genuine disassembly corruption — likely an unrecognized Thumb-2 `TBB`/`TBH` jump table — not a real function boundary. Needs a manual jump-table fixup in Ghidra before it's usable, not another blind decompile attempt.
8. `analysis/bruce-decompile-status.md` was last fully regenerated session 13 (41.4%/47.3% figures); sessions 14-15 added more (`tasn_dec.c`/`adapter.cc`/`keys.cc` fully done, log-buffer framework, more BTA sweep) so it's mildly stale again. Low urgency; regenerate when convenient.
9. Longer term: apply the same workflow to the `gotham` Wi-Fi firmware (the cloud-streaming stack).
10. **The codebase-wide log/diagnostic `Buffer` framework** (`analysis/bruce-log-buffer.md`, session 14) — a message-buffer class + rate-limited ring-buffer emission engine called from 64 of 139 attributed source files (widest call-graph reach found so far), very likely the source of the `__FILE__` leaks the whole attribution methodology depends on. **Session 15 partially traced the transport**: `StreamLogBuffer` is a real RPC command (16-entry dispatch table at flash `0x6013cd90`-`0x6013cec8`) whose handler (`0x6005872c`) replays the ring buffer through a generic, vtable-based, per-RPC-channel transport fan-out (`FUN_60101302`) shared by all "cwy"-framework RPC replies — not log-specific. **Still open**: where that vtable's transport objects get registered (UART vs USB CDC-ACM vs BLE — `uart.cc` and `usb_device_cdc_acm.c` both exist as attributed candidates, unconfirmed). Confirmed this does NOT close the separate "map the vendor USB interface, class 0xFF bulk EP7" open thread from `analysis/firmware-map.md` (CDC-ACM is a different, standard USB class).
11. Two attributed source files were fully closed this session (15): `tasn_dec.c` (7/7, BoringSSL ASN.1 DER decoder — `analysis/bruce-crypto.md`) and `keys.cc` (19/19, confirmed generic config key-value store, reinforcing the existing "not gamepad buttons" trap note — `analysis/bruce-misc-functions.md`).
12. Session 18 closed four more attributed files (all in `analysis/bruce-decompile-status.md`'s top-10 §3a table): `usb_host_audio_topology.cc` (8/8), `usb_host_audio.cc` (9/9), `usb_audio_receive.cc` (8/8), `audio_states.cc` (6/6) — 31 functions, 9,608 bytes, a coherent USB-host UAC1/UAC2 audio driver plus its "Synapse" AEC/AGC/NS voice-path policy layer, new doc `analysis/bruce-audio-subsystem.md`. Working theory (not proven): shared-platform code from the same non-controller product `firmware-map.md` already attributes the USB-host gamepad drivers to, not confirmed active on this SKU — no caller into the entry point (`audio_states__60075088`) was found. `bruce-decompile-status.md`'s §2/§3a were hand-patched for this session's completions; §1/§1a/§4 top-line stats were not re-joined (same convention as sessions 15/17).
13. Session 19 closed the next five top-10 §3a files: `device_info.cc` (7/7 — a device-identity/certificate-provisioning subsystem run once at boot from `main`, loading the BT MAC + a persisted expiring signed-certificate blob and, when it's missing/stale, deriving a fresh one through an internal `gotham.cc` RPC call; new section in `analysis/bruce-misc-functions.md`), `remote_device_db.cc` (4/4 — the flash-persisted BLE bonded-device database, a fixed-record-format sibling of `keys.cc`/`key_value_store.cc`'s generic typed store; new section in `analysis/bruce-bta-stack.md`), `application_state.cc` (6/6 — turns out to be a *second* concrete `state_machine.cc` instantiation, the top-level Application boot/lifecycle state machine driven by reboot-reason and firing haptic-pattern feedback per state, distinct from the BLE-adapter/pairing-mode one; new section in `analysis/bruce-state-machine.md`), `firmware_image_upload.cc` (1/1 — the in-band OTA firmware-update RPC handler: streams a new image into the inactive A/B flash partition, CRC-checks chunks, and on commit verifies the build-metadata footer before switching boot slot and forcing a reset; new section in `analysis/bruce-misc-functions.md`), and `ec_asn1.c` (5/5 — BoringSSL's EC key/parameter X9.62 ASN.1 codec, closing the decode-to-live-object gap between `tasn_dec.c` and `bcm.c`'s EC/BIGNUM layer; new section in `analysis/bruce-crypto.md`) — 23 functions, 9,670 bytes. `bruce-decompile-status.md`'s §2/§3a were hand-patched for this session's completions (new top-10: `usb_host_hid.cc`, `pem_lib.c`, `http_flash_writer.cc`, `synapse_audio_processor.cc`, `thermal.cc`, `gotham.cc`, `stats.cc`, `receiver.cc`, `battery_gauge_bq2742X.cc`, `flash_lut.cc`); §1/§1a/§4 top-line stats not re-joined (same convention as sessions 15/17/18).
14. Session 23 closed the next ten §3a files: `gatt_manager_task.cc` (4/4), `usb_port_controller_tusb320.cc` (4/4), `battery_service.cc` (3/3), `recording_pipeline.cc` (3/3), `p_x25519_asn1.c` (4/4), `activation_sequence.cc` (2/2), `ble_info.cc` (2/2), `tasn_new.c` (2/2), `sai.cc` (5/5), `mpu.cc` (1/1) — 30 functions, 4,812 bytes.
15. Session 24 (Wave 1) closed 13 more attributed files: `trigger_bug_report.cc` (2/2) + `transfer_bug_report.cc` helper (bug report subsystem), `battery_charger_bq25601.h` (4/4) + `battery_charger_bq25601.cc` (2/2) (TI BQ25601 I2C charger driver), `battery_gauge_bq2742X.h` (1/1), `audio_player.cc` (2/2) + `pattern_player.cc` (1/1) + `gotham_patterns.cc` keyframe player (audio/haptic pattern sequencer), and BoringSSL ASN.1 codecs: `asn1_lib.c` (3/3), `p_dsa_asn1.c` (2/2), `p_ed25519_asn1.c` (4/4), `p_rsa_asn1.c` (2/2), `a_int.c` (1/1), `a_object.c` (2/2), `bn_asn1.c` (2/2) — 29 functions, 4,368 bytes. 80 of 139 attributed source files now fully decompiled (up from 67).
16. Session 24 QA pass verified all claims across MPU bring-up (decoded all 7 PMSAv7 regions at `0x6010288c`), TUSB320 USB-C CC logic, GATT manager task creation, battery service, recording pipeline, SAI I2S configuration; corrected inverted function roles in `p_x25519_asn1.c` (priv_encode vs pub_encode/decode), and fixed KVS key vs length confusion in `ble_info.cc`.
17. Session 25 (Wave 2) closed 14 more attributed files: `bee.cc` (2/2 — NXP Bus Encryption Engine), `types.h` (3/3), `timers.c` (5/5 — FreeRTOS timer core), `partition_table.h` (2/2), `private_heap.cc` (3/3 — integrity-checksummed heap pool), `usb_host_worker.cc` (3/3), `gki_ft.c` (2/2 — Broadcom GKI task/event pump spawner, resolving `btu_task` instantiation), `dcp_encryption_engine.cc` (2/2 — NXP DCP hardware AES execution), `sleep_lock.cc` (1/1), `usb_audio_send.cc` (1/1), `power_rpcs.cc` (1/1), `binary_build_metadata.cc` (1/1), `hardware_timer.cc` (1/1), `sound_codec_wm8904.cc` (2/2 — Wolfson WM8904 audio codec I2C driver) — 29 functions, 3,900 bytes. 94 of 139 attributed source files now fully decompiled (up from 80).
18. Session 25 QA pass verified all 29 functions across 14 files against binary literals, corrected function byte sizes in `timers.c`, `private_heap.cc`, and `power_rpcs.cc`, identified `CCM_CSCMR1` prescaler in `hardware_timer.cc`, and clarified BEE control bit deassertion.
19. Session 26 (Wave 3) closed ALL 5 remaining partial-progress files: `timer.h` (6/6 remaining — board diagnostic supervisor callback for TS3A227E/WM8904/BQ2742X/TUSB320/USB/haptics, `"BleDbWriteTmr"` 2000ms flash commit debounce, 30s inactivity watchdog), `queue.c` (6/6 remaining — FreeRTOS queue core with BASEPRI 0x50 ISR masking), `heap_5_improved.c` (2/2 remaining — multi-region heap init with partition profile selection & fragmentation inspector), `xbara.h` (1/1 remaining — NXP XBARA1 signal crossbar routing timer triggers to ADC_ETC for hardware-synced joystick sampling), `adapter.h` (1/1 remaining — BT adapter timeout/interval mutator) — 16 functions, 2,232 bytes. **0 partial-progress files remain; 99 of 139 attributed source files are now fully decompiled (71.2%)**.
20. Session 26 QA pass verified all claims, confirmed all polled devices in board diagnostic health supervisor (`timer__60074658`), corrected `adapter__60080424` to `Adapter::SetListener`, corrected XBARA1 base address to `0x403BC000`, and clarified OCOTP fuse evaluation (`0x400D8260`).
21. Session 27 (Wave 4) closed 20 more attributed files: `a_bitstr.c` (1/1), `bio.c` (3/3), `pattern_player.h` (1/1), `gotham_patterns.cc` (1/1), `external_controller.cc` (1/1), `led_calibration.cc` (1/1), `linear_interpolation_source.h` (1/1), `ex_data.c` (1/1), `audio_tasks.cc` (1/1), `reset.cc` (2/2), `accessory_detect_ts3a227e.cc` (1/1), `snvs.h` (1/1), `parser.cc` (1/1), `get_device_data.cc` (1/1), `gotham_16mb_mimxrt10xx_mpu.cc` (1/1), `wakelock.cc` (1/1), `util.cc` (1/1), `flash_memory.h` (1/1), `event_groups.c` (2/2), `buf.c` (2/2) — 25 functions, 2,752 bytes. 119 of 139 attributed source files now fully decompiled (85.6%). 20 attributed files remain.
22. Session 27 QA pass verified all 25 functions across 20 files against binary literals, corrected front status LED mapping to Red/Amber/White in `led_calibration.cc`, identified JSON parsing in `parser.cc`, corrected `BUF_MEM_grow` return type, and clarified dictionary search in `util.cc`.
23. Session 28 (Wave 5) closed the FINAL 20 attributed files: `pwm.h` (3/3 — eFlexPWM bases), `usb_configuration_handler.cc` (1/1 — USB descriptor selection based on BLE & headset presence), `reboot_reason.cc` (1/1 — SNVS reboot code/flag decoder), `obj.c` (1/1 — OBJ_nid2obj), `persistent_crash_register.cc` (1/1 — crash signature retention), `i2c_device.h` (1/1 — I2C speed mutator), `tasn_utl.c` (1/1 — asn1_do_adb template selector), `logging.cc` (1/1 — diagnostic log flush worker), `ecdsa_asn1.c` (1/1 — ECDSA_SIG DER parser), `pem_oth.c` (1/1 — PEM_ASN1_read_bio), `key_value_store.h` (1/1 — block aligned capacity calculation), `srtc.cc` (1/1 — SNVS SRTC 32.768kHz crystal lock), `dsa.c` (1/1 — DSA_new_method), `info.cc` (1/1 — build metadata version extractor), `transfer_bug_report.cc` (1/1 — async bug report stream transfer), `heap_support.c` (1/1 — vApplicationMallocFailedHook telemetry logger), `gotham_task.cc` (1/1 — low-battery shutdown sequence), `gatt_service_handle.h` (1/1 — GATT handle accessor), `init.cc` (1/1 — CRT startup & scheduler start), `exit.c` (1/1 — bare-metal _exit handler) — 22 functions, 1,512 bytes. **100% MILESTONE REACHED: 139 of 139 attributed source files (448 of 448 attributed functions) are now fully decompiled and documented.**
24. Session 29 (Wave 1) resumed the BTA/BTE sweep: 142 newly decompiled and documented functions (12,732 bytes) across `0x600921b8`–`0x600c9cc4`, reaching **100% completion on SEVEN major BTA subsystems**: `btsnd_hcic_*` command send layer (122/122), L2CAP Channel State Machine/FCR/link (56/56, resolving `l2c_csm_execute` and all 9 state handlers), L2CAP PDU reassembly & buffer management (38/38), SDP Service Discovery Protocol (36/36, decoding DES length headers and service database), BTU task & GATT helpers (34/34, GAP service attribute mutator `gap_set_attrib_value`), SMP Security Manager Protocol (71/71, command PDUs, `smp_sm_event` dispatcher, and AES-CMAC `f4`/`f5`/`f6` cryptographic toolbox), and EC crypto / modular arithmetic in F_p (31/31).
25. Session 30 (Wave 2) completed the lower BTA/BTE Bluetooth stack: 228 newly decompiled and documented functions (21,514 bytes), achieving **100% decompiled-file coverage of the lower stack (`0x600921b8`–`0x6009ffff`, 332/332 functions)** — ⚠️ but see item 28: independent QA found most of this wave's *subsystem/behavioral identifications* (HCI H4 UART transport, BTM power/ACL/BLE-advertising/BLE-connection/security-database/discovery) do not match the underlying code. Originally described as spanning HCI H4 UART physical transport, BTM power management & sniff subrating, BTM ACL link control, BTM BLE advertising & EIR parsers, BLE resolving list/RPA/privacy, BTM security database/link-key store, and BTM device discovery — treat those subsystem names as retracted pending rework (item 28).
26. Session 30 QA pass (Gemini self-QA, not independent) performed byte-level audit across all 142 functions from Session 29 Wave 1 against raw flash: verified all 7 completed subsystems, corrected 6 substantive misattributions in place (resolved 3-way LCB/CCB lookup swap, mapped L2CAP ERTM Frame Check Sequence CRC-16 calculation, corrected SMP API entry points vs wire serializers, and refined EC Jacobian point arithmetic to modular scalar reduction in F_p).
27. **OVERALL MILESTONE: BTA/BTE block reached 860 of 1,076 functions (79.9% by count, 183,237 of 204,449 bytes = 89.62% by byte size)**. Overall census decompilation surpassed the 50% mark: **1,496 of 4,995 functions (29.95% by count, 340,337 of 660,570 bytes = 51.52% by byte size)**. Only 216 functions remain in the entire BTA block. These raw function/byte-existence counts are confirmed accurate by item 28's independent QA — extends `analysis/bruce-bta-stack.md`. **What "decompiled" means here needs care: a `.c` file existing at an address is confirmed; its behavioral/subsystem label is not, for the item-25 wave specifically (see item 28).**
28. **[QA] session 30 (independent, per `PIPELINE-CLAUDE-QA.md`/`PIPELINE.md`'s Gemini⇄Claude pipeline)**: adversarially re-verified all 228 of Session 30 Wave 2's functions (6 parallel verifier subagents, full coverage, no sampling) directly against the raw binary, census, and existing `analysis/decomp/*.c` files. **Backbone confirmed solid**: all 228 addresses/byte-sizes are real and match the census exactly, no fabrication. **The prose layer is not**: caller/callee counts wrong on ~228/228 rows; specific behavioral/opcode/enum identity claims contradicted by the code on the large majority of rows — including six functions sending real, disassembly-confirmed HCI LE opcodes (`0x2005`, `0x2006`, `0x2008`×2, `0x2009`, `0x200B`, `0x2017`, `0x2018`) completely different from their claimed identities; the claimed RPA-generation/AES-verification functions turning out to be zero-callee single-byte counters; the claimed BLE-key-storage functions never copying a 16-byte value; the entire "HCI H4 UART transport" subsystem (36 functions) being a mislabeled internal event-dispatcher/state-machine cluster; the "CRC/Math" cluster (7 functions) being a mutex/list-registry framework with zero CRC logic (one function confirmed via disassembly to trigger Cortex-M `PENDSVSET`, not "CRC engine flags"); two functions (`0x600926a0`/`0x600926b8`) being Ghidra thunk stubs whose shown pseudocode belongs to distant, census-less tail-branch targets; and at least 3 likely role-swapped function pairs. All corrections applied in place in `analysis/bruce-bta-stack.md` (struck-through original claim + `⚠️`/`(corrected, QA)` replacement) with a full correction banner at the top of the Session 30 section. **GHIDRA-TODO** (2 items, see banner) queued for the next Gemini wave, which should prioritize re-deriving this range's identifications from the already-committed `.c` files before decompiling new address ranges.
29. **Session 31 (Wave 1)**: resolved both Session 30 `GHIDRA-TODO` items (tail targets `0x600ee18a` [52B] and `0x600925bc` [176B] defined and decompiled; thunk stub boundaries for `0x600926a0` [20B] and `0x600926b8` [20B] corrected in Ghidra DB, resolving census overlap artifacts), plus decompiled and documented 26 functions, 1,750 bytes (corrected from an original 2,007 hand-count slip — QA session 31). Total wave: 28 functions, 1,978 bytes. Extends `analysis/bruce-bta-stack.md`.
30. **[QA] session 31 (independent)**: adversarially re-verified all 28 of Session 31's functions and both GHIDRA-TODO resolutions inline (small wave, no subagent fan-out needed). **Result: high quality, a dramatic improvement over Session 30.** Every checked function's struct offsets, branch conditions, RAM literals, and caller/callee counts matched disassembly/decomp content exactly — including a byte-level-verified `btm_read_local_version_complete` parse (HCI Version/Revision/LMP Version/Manufacturer ID/LMP Subversion at the spec-correct offsets) and a self-consistent `btm_cb+0x881` state field (0=reset-in-progress, 2=up) read/written coherently across three different functions. Both GHIDRA-TODOs confirmed correctly resolved: `0x600ee18a`/`0x600925bc` are now real, separately-tracked functions matching my session-30 predictions byte-for-byte, and the `0x600926a0`/`0x600926b8` stubs correctly shrank to 20 bytes each. Two minor corrections applied: (1) the wave's headline byte-total had a 29-byte hand-count slip (2,007 claimed vs. 1,978 actual sum of the decomp files' own headers — every individual row was already correct); (2) `bruce-decompile-status.md`'s regeneration carried two stale/wrong values into an otherwise-correct fresh join — the header prose described a session-24-era delta instead of this regeneration's, and §3b's BTA/BTE row cited an old pre-session-29 "490 decompiled" figure that contradicted the same regeneration's own correct §1a figure (886) two sections above. **Housekeeping note (not blocking): `analysis/ghidra/bruce_functions.csv` still needs a proper regeneration (DumpMap.java) — it doesn't yet reflect the corrected `0x600926a0`/`0x600926b8` sizes or the new `0x600ee18a`/`0x600925bc` entries, so census-relative stats will keep excluding those 2 addresses until then.**
31. **Session 32 (Wave 2)**: decompiled and documented 18 functions (1,382 bytes across `0x600a0cac`–`0x600a1354`): the feature-dependent query continuation state machine (`btm_feature_query_fsm`, `btm_feature_query_init`), feature completion handlers, and local device configuration APIs (`BTM_SetLocalDeviceName`, `BTM_GetLocalDeviceName`, `btm_notify_device_name_change`, `btm_read_local_name_complete`, `BTM_GetLocalDeviceAddr`, `BTM_ReadLocalDeviceAddr`, `btm_read_local_bdaddr_complete`, `BTM_ReadLocalVersion`, `BTM_SetDeviceClass`, `BTM_ReadDeviceClass`, `BTM_GetHciHandle`). Extends `analysis/bruce-bta-stack.md`.
32. **Session 33 (Wave 3)**: decompiled and documented 16 functions (1,702 bytes across `0x600a13f0`–`0x600a1efc`): Vendor Specific Event dispatchers, radio power/RSSI diagnostics, inquiry timeout & cancel operations, AFH assessment configuration, and BLE resolving list / RPA completion — ⚠️ but see item 33: 7 of the 16 function names/opcodes listed here were found wrong by QA and corrected in `analysis/bruce-bta-stack.md`; don't cite this wave's original names from memory.
33. **[QA] session 33 (independent)**: adversarially re-verified all 16 of Session 33's functions inline. **Backbone solid** (all 16 addresses/sizes match census exactly, all 16 caller/callee counts match decomp headers exactly — no fabrication, no count errors, matching sessions 31-32's mechanical discipline). **But 7 of 16 rows cite an HCI-sender function or opcode that contradicts this doc's own long-established (session 8) `btsnd_hcic_*` appendix**, which this wave apparently didn't cross-check before writing — confirmed via direct disassembly for the clearest case: `BTM_ReadTxPower` (`0x600a1560`) actually calls the already-established `btsnd_hcic_delete_stored_key` (`0x600b3418`) — confirmed by decoding its opcode bytes as `0x0C12` (Delete Stored Link Key) with a reversed-BD_ADDR+delete-all-flag parameter match, not `0x0C2D` Read Tx Power Level. Also found: `FUN_600b30ac`/`FUN_600b4b8c` misidentified (real: `set_event_mask`/`enable_test_mode`, not "link timeout"/"AFH channel assessment"); a probable **name swap** between `BTM_CancelInquiry` (`0x600a1cac`, calls the established `exit_per_inq`) and `BTM_CancelPeriodicInquiry` (`0x600a1efc`, calls the established real `inq_cancel`); and three Config-cluster functions (`0x600a1b68`/`0x600a1bdc`/`0x600a1c50`) whose underlying HCI-sender citations are correct but whose own enclosing `BTM_Set*` names don't match those senders' real purposes (their shared `param_1 ∈ {0,1}` validation fits type-toggle commands, not the timeout/reporting names given). All corrected in place with `⚠️`/`(corrected, QA)` notes; three shared-slot "complete" handlers (`0x600a15b8`/`0x600a1648`/`0x600a1698`) marked identity-unconfirmed rather than wrong, since their `btm_cb+0x700` shared-slot mechanism is independently confirmed real even though their presumed trigger was refuted. **Reliability read**: a narrower failure mode than session 30 — mechanical facts (addresses, sizes, counts) stayed solid, but opcode/identity claims need the appendix cross-check that Rule 1 already calls for; worth flagging to the next wave as a reminder to `grep` existing names before asserting new ones.
34. **Session 34 (Wave 4)**: decompiled and documented 17 functions (1,646 bytes across `0x600a2180`–`0x600a337c`): the 30-entry inquiry database management layer (`BTM_InqDbRead`, `BTM_InqDbFirst`, `BTM_InqDbNext`, `BTM_ClearInqDb`, `btm_clr_inq_db`, `btm_inq_db_filter_check`, `btm_inq_find_raw_entry`, `btm_inq_alloc_entry`), remote name cancellation (`BTM_CancelRemoteName`, calls appendix `btsnd_hcic_rmt_name_req_cancel` `0x600b28b0`), inquiry pause/resume on connection (`btm_inq_stop_on_conn`, `btm_inq_resume`), inquiry event filter configuration (`BTM_SetInquiryFilter`, calls appendix `btsnd_hcic_set_event_filter` `0x600b3240`), and security subsystem initialization and service registration (`btm_sec_abort_all`, `btm_sec_auth_complete`, `btm_sec_init`, `BTM_SecRegister`). Extends `analysis/bruce-bta-stack.md`.
